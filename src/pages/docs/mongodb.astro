---
import DocsLayout from '../../layouts/DocsLayout.astro';

const content = `<h1>MongoDB</h1>
<ul>
<li><a href="#introduction">簡介</a></li>
<li><a href="#installation">安裝</a><ul>
<li><a href="#mongodb-driver">MongoDB 驅動程式</a></li>
<li><a href="#starting-a-mongodb-server">啟動 MongoDB 伺服器</a></li>
<li><a href="#install-the-laravel-mongodb-package">安裝 Laravel MongoDB 套件</a></li>
</ul>
</li>
<li><a href="#configuration">配置</a></li>
<li><a href="#features">功能特性</a></li>
</ul>
<p><a name="introduction"></a></p>
<h2>簡介</h2>
<p><a href="https://www.mongodb.com/resources/products/fundamentals/why-use-mongodb">MongoDB</a> 是最受歡迎的 NoSQL 文件導向資料庫之一，因其高寫入負載（適用於分析或物聯網）和高可用性（易於設定具有自動故障轉移的複製集）而被廣泛使用。它還可以輕鬆地對資料庫進行分片以實現水平擴展，並具有強大的查詢語言用於聚合、文字搜尋或地理空間查詢。</p>
<p>與 SQL 資料庫將資料儲存在行或列的表格中不同，MongoDB 資料庫中的每條記錄都是以 BSON（資料的二進制表示）描述的文件。應用程式可以以 JSON 格式檢索這些資訊。它支援多種資料類型，包括文件、陣列、嵌入式文件和二進制資料。</p>
<p>在 Laravel 中使用 MongoDB 之前，我們建議透過 Composer 安裝並使用 <code>mongodb/laravel-mongodb</code> 套件。<code>laravel-mongodb</code> 套件由 MongoDB 官方維護，雖然 MongoDB 透過 MongoDB 驅動程式原生支援 PHP，但 <a href="https://www.mongodb.com/docs/drivers/php/laravel-mongodb/">Laravel MongoDB</a> 套件提供了與 Eloquent 和其他 Laravel 功能更豐富的整合：</p>
<pre><code class="language-shell">composer require mongodb/laravel-mongodb
</code></pre>
<p><a name="installation"></a></p>
<h2>安裝</h2>
<p><a name="mongodb-driver"></a></p>
<h3>MongoDB 驅動程式</h3>
<p>要連線到 MongoDB 資料庫，需要 <code>mongodb</code> PHP 擴充套件。如果您使用 <a href="https://herd.laravel.com">Laravel Herd</a> 在本機開發或透過 <code>php.new</code> 安裝 PHP，您的系統上已經安裝了此擴充套件。然而，如果您需要手動安裝擴充套件，可以透過 PECL 進行：</p>
<pre><code class="language-shell">pecl install mongodb
</code></pre>
<p>有關安裝 MongoDB PHP 擴充套件的更多資訊，請查看 <a href="https://www.php.net/manual/en/mongodb.installation.php">MongoDB PHP 擴充套件安裝說明</a>。</p>
<p><a name="starting-a-mongodb-server"></a></p>
<h3>啟動 MongoDB 伺服器</h3>
<p>MongoDB Community Server 可用於在本機執行 MongoDB，並可在 Windows、macOS、Linux 上安裝或作為 Docker 容器使用。有關如何安裝 MongoDB，請參閱 <a href="https://docs.mongodb.com/manual/administration/install-community/">MongoDB 官方 Community 安裝指南</a>。</p>
<p>MongoDB 伺服器的連接字串可以在 <code>.env</code> 檔案中設定：</p>
<pre><code class="language-ini">MONGODB_URI=&quot;mongodb://localhost:27017&quot;
MONGODB_DATABASE=&quot;laravel_app&quot;
</code></pre>
<p>要在雲端託管 MongoDB，請考慮使用 <a href="https://www.mongodb.com/cloud/atlas">MongoDB Atlas</a>。
要從您的應用程式在本機存取 MongoDB Atlas 叢集，您需要在專案的 IP 存取清單中<a href="https://www.mongodb.com/docs/atlas/security/add-ip-address-to-list/">添加您自己的 IP 地址</a>。</p>
<p>MongoDB Atlas 的連接字串也可以在 <code>.env</code> 檔案中設定：</p>
<pre><code class="language-ini">MONGODB_URI=&quot;mongodb+srv://&lt;username&gt;:&lt;password&gt;@&lt;cluster&gt;.mongodb.net/&lt;dbname&gt;?retryWrites=true&amp;w=majority&quot;
MONGODB_DATABASE=&quot;laravel_app&quot;
</code></pre>
<p><a name="install-the-laravel-mongodb-package"></a></p>
<h3>安裝 Laravel MongoDB 套件</h3>
<p>最後，使用 Composer 安裝 Laravel MongoDB 套件：</p>
<pre><code class="language-shell">composer require mongodb/laravel-mongodb
</code></pre>
<blockquote>
<p>[!NOTE]
如果未安裝 <code>mongodb</code> PHP 擴充套件，此套件的安裝將會失敗。PHP 配置在 CLI 和 Web 伺服器之間可能不同，因此請確保在兩種配置中都啟用了該擴充套件。</p>
</blockquote>
<p><a name="configuration"></a></p>
<h2>配置</h2>
<p>您可以透過應用程式的 <code>config/database.php</code> 配置檔案來配置 MongoDB 連接。在此檔案中，添加一個使用 <code>mongodb</code> 驅動程式的 <code>mongodb</code> 連接：</p>
<pre><code class="language-php">&#39;connections&#39; =&gt; [
    &#39;mongodb&#39; =&gt; [
        &#39;driver&#39; =&gt; &#39;mongodb&#39;,
        &#39;dsn&#39; =&gt; env(&#39;MONGODB_URI&#39;, &#39;mongodb://localhost:27017&#39;),
        &#39;database&#39; =&gt; env(&#39;MONGODB_DATABASE&#39;, &#39;laravel_app&#39;),
    ],
],
</code></pre>
<p><a name="features"></a></p>
<h2>功能特性</h2>
<p>配置完成後，您可以在應用程式中使用 <code>mongodb</code> 套件和資料庫連接來利用各種強大功能：</p>
<ul>
<li><a href="https://www.mongodb.com/docs/drivers/php/laravel-mongodb/current/eloquent-models/">使用 Eloquent</a>，模型可以儲存在 MongoDB 集合中。除了標準的 Eloquent 功能外，Laravel MongoDB 套件還提供了額外的功能，如嵌入式關聯。該套件還提供對 MongoDB 驅動程式的直接存取，可用於執行原始查詢和聚合管線等操作。</li>
<li>使用查詢建構器<a href="https://www.mongodb.com/docs/drivers/php/laravel-mongodb/current/query-builder/">編寫複雜查詢</a>。</li>
<li>使用向量嵌入和 <code>vectorSearch</code> Eloquent 方法進行<a href="https://www.mongodb.com/docs/drivers/php/laravel-mongodb/current/fundamentals/vector-search/">相似性 / 向量搜尋</a>。</li>
<li><code>mongodb</code> <a href="https://www.mongodb.com/docs/drivers/php/laravel-mongodb/current/cache/">快取驅動程式</a>已優化以使用 MongoDB 功能，如 TTL 索引來自動清除過期的快取項目。</li>
<li>使用 <code>mongodb</code> 佇列驅動程式<a href="https://www.mongodb.com/docs/drivers/php/laravel-mongodb/current/queues/">派發和處理佇列任務</a>。</li>
<li>透過 <a href="https://flysystem.thephpleague.com/docs/adapter/gridfs/">Flysystem 的 GridFS 適配器</a><a href="https://www.mongodb.com/docs/drivers/php/laravel-mongodb/current/filesystems/">在 GridFS 中儲存檔案</a>。</li>
<li>使用 <code>mongodb</code> Scout 引擎進行<a href="https://www.mongodb.com/docs/drivers/php/laravel-mongodb/current/scout/">全文搜尋</a>。</li>
<li>大多數使用資料庫連接或 Eloquent 的第三方套件都可以與 MongoDB 一起使用。</li>
</ul>
<p>要繼續學習如何使用 MongoDB 和 Laravel，請參閱 MongoDB 的 <a href="https://www.mongodb.com/docs/drivers/php/laravel-mongodb/current/quick-start/">快速入門指南</a>。</p>
`;
---

<DocsLayout title="MongoDB" currentPath="mongodb">
  <div class="mb-8">
    <nav class="text-sm text-text-secondary mb-4">
      <a href="/" class="hover:text-accent">首頁</a>
      <span class="mx-2">/</span>
      <span>文件</span>
      <span class="mx-2">/</span>
      <span>MongoDB</span>
    </nav>
    
    <h1 class="text-4xl font-bold mb-4">MongoDB</h1>
    
    <div class="bg-bg-secondary border border-border rounded-lg p-4 mb-6">
      <p class="text-text-secondary">
        📝 此頁面為 Laravel 官方文檔的繁體中文翻譯。
        <a href="https://laravel.com/docs/13.x/mongodb" class="text-accent" target="_blank" rel="noopener">
          查看原始英文版本
        </a>
      </p>
    </div>
  </div>

  <div class="prose prose-invert max-w-none" set:html={content}></div>
</DocsLayout>
