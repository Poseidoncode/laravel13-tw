---
import DocsLayout from '../../layouts/DocsLayout.astro';

const content = `<h1>Laravel Homestead</h1>
<ul>
<li><a href="#introduction">簡介</a></li>
<li><a href="#installation-and-setup">安裝和設定</a><ul>
<li><a href="#first-steps">第一步</a></li>
<li><a href="#configuring-homestead">配置 Homestead</a></li>
<li><a href="#configuring-nginx-sites">配置 Nginx 站點</a></li>
<li><a href="#configuring-services">配置服務</a></li>
<li><a href="#launching-the-vagrant-box">啟動 Vagrant Box</a></li>
<li><a href="#per-project-installation">按專案安裝</a></li>
<li><a href="#installing-optional-features">安裝可選功能</a></li>
<li><a href="#aliases">別名</a></li>
</ul>
</li>
<li><a href="#updating-homestead">更新 Homestead</a></li>
<li><a href="#daily-usage">日常使用</a><ul>
<li><a href="#connecting-via-ssh">透過 SSH 連接</a></li>
<li><a href="#adding-additional-sites">添加其他站點</a></li>
<li><a href="#environment-variables">環境變數</a></li>
<li><a href="#ports">端口</a></li>
<li><a href="#php-versions">PHP 版本</a></li>
<li><a href="#connecting-to-databases">連接資料庫</a></li>
<li><a href="#database-backups">資料庫備份</a></li>
<li><a href="#configuring-cron-schedules">配置 Cron 排程</a></li>
<li><a href="#configuring-mailpit">配置 Mailpit</a></li>
<li><a href="#configuring-minio">配置 Minio</a></li>
<li><a href="#laravel-dusk">Laravel Dusk</a></li>
<li><a href="#sharing-your-environment">共享您的環境</a></li>
</ul>
</li>
<li><a href="#debugging-and-profiling">除錯和效能分析</a><ul>
<li><a href="#debugging-web-requests">使用 Xdebug 除錯 Web 請求</a></li>
<li><a href="#debugging-cli-applications">除錯 CLI 應用程式</a></li>
<li><a href="#profiling-applications-with-blackfire">使用 Blackfire 進行效能分析</a></li>
</ul>
</li>
<li><a href="#network-interfaces">網路介面</a></li>
<li><a href="#extending-homestead">擴展 Homestead</a></li>
<li><a href="#provider-specific-settings">供應商特定設定</a><ul>
<li><a href="#provider-specific-virtualbox">VirtualBox</a></li>
</ul>
</li>
</ul>
<p><a name="introduction"></a></p>
<h2>簡介</h2>
<blockquote>
<p>[!WARNING]
Laravel Homestead 是一個舊版套件，目前已停止維護。<a href="/docs/sail">Laravel Sail</a> 可作為現代化的替代方案。</p>
</blockquote>
<p>Laravel 致力於使整個 PHP 開發體驗令人愉悅，包括您的本機開發環境。<a href="https://github.com/laravel/homestead">Laravel Homestead</a> 是一個官方的、預打包的 Vagrant box，為您提供了一個美妙的開發環境，而無需在您的本機機器上安裝 PHP、Web 伺服器或任何其他伺服器軟體。</p>
<p><a href="https://www.vagrantup.com">Vagrant</a> 提供了一種簡單、優雅的方式來管理和供應虛擬機器。Vagrant box 是完全可處置的。如果出了問題，您可以在幾分鐘內銷毀並重新建立 box！</p>
<p>Homestead 可在任何 Windows、macOS 或 Linux 系統上運行，並包含 Nginx、PHP、MySQL、PostgreSQL、Redis、Memcached、Node 和您開發出色 Laravel 應用程式所需的所有其他軟體。</p>
<blockquote>
<p>[!WARNING]
如果您使用 Windows，您可能需要啟用硬體虛擬化（VT-x）。通常可以透過您的 BIOS 啟用。如果您在 UEFI 系統上使用 Hyper-V，您可能還需要禁用 Hyper-V 才能存取 VT-x。</p>
</blockquote>
<p><a name="included-software"></a></p>
<h3>內建軟體</h3>
<style>
    #software-list > ul {
        column-count: 2; -moz-column-count: 2; -webkit-column-count: 2;
        column-gap: 5em; -moz-column-gap: 5em; -webkit-column-gap: 5em;
        line-height: 1.9;
    }
</style><div id="software-list" markdown="1"><ul>
<li>Ubuntu 22.04</li>
<li>Git</li>
<li>PHP 8.3</li>
<li>PHP 8.2</li>
<li>PHP 8.1</li>
<li>PHP 8.0</li>
<li>PHP 7.4</li>
<li>PHP 7.3</li>
<li>PHP 7.2</li>
<li>PHP 7.1</li>
<li>PHP 7.0</li>
<li>PHP 5.6</li>
<li>Nginx</li>
<li>MySQL 8.0</li>
<li>lmm</li>
<li>Sqlite3</li>
<li>PostgreSQL 15</li>
<li>Composer</li>
<li>Docker</li>
<li>Node（含 Yarn、Bower、Grunt 和 Gulp）</li>
<li>Redis</li>
<li>Memcached</li>
<li>Beanstalkd</li>
<li>Mailpit</li>
<li>avahi</li>
<li>ngrok</li>
<li>Xdebug</li>
<li>XHProf / Tideways / XHGui</li>
<li>wp-cli</li>
</ul>
</div><p><a name="optional-software"></a></p>
<h3>可選軟體</h3>
<style>
    #software-list > ul {
        column-count: 2; -moz-column-count: 2; -webkit-column-count: 2;
        column-gap: 5em; -moz-column-gap: 5em; -webkit-column-gap: 5em;
        line-height: 1.9;
    }
</style><div id="software-list" markdown="1"><ul>
<li>Apache</li>
<li>Blackfire</li>
<li>Cassandra</li>
<li>Chronograf</li>
<li>CouchDB</li>
<li>Crystal &amp; Lucky Framework</li>
<li>Elasticsearch</li>
<li>EventStoreDB</li>
<li>Flyway</li>
<li>Gearman</li>
<li>Go</li>
<li>Grafana</li>
<li>InfluxDB</li>
<li>Logstash</li>
<li>MariaDB</li>
<li>Meilisearch</li>
<li>MinIO</li>
<li>MongoDB</li>
<li>Neo4j</li>
<li>Oh My Zsh</li>
<li>Open Resty</li>
<li>PM2</li>
<li>Python</li>
<li>R</li>
<li>RabbitMQ</li>
<li>Rust</li>
<li>RVM (Ruby Version Manager)</li>
<li>Solr</li>
<li>TimescaleDB</li>
<li>Trader <small>(PHP extension)</small></li>
<li>Webdriver &amp; Laravel Dusk Utilities</li>
</ul>
</div><p><a name="installation-and-setup"></a></p>
<h2>安裝和設定</h2>
<p><a name="first-steps"></a></p>
<h3>第一步</h3>
<p>在啟動 Homestead 環境之前，您必須安裝 <a href="https://developer.hashicorp.com/vagrant/downloads">Vagrant</a> 以及以下支援的供應商之一：</p>
<ul>
<li><a href="https://www.virtualbox.org/wiki/Download_Old_Builds_6_1">VirtualBox 6.1.x</a></li>
<li><a href="https://www.parallels.com/products/desktop/">Parallels</a></li>
</ul>
<p>所有這些軟體套件都為所有流行作業系統提供了易於使用的視覺化安裝器。</p>
<p>要使用 Parallels 供應商，您需要安裝 <a href="https://github.com/Parallels/vagrant-parallels">Parallels Vagrant 外掛</a>。它是免費的。</p>
<p><a name="installing-homestead"></a></p>
<h4>安裝 Homestead</h4>
<p>您可以透過將 Homestead 儲存庫克隆到您的主機上來安裝 Homestead。考慮將儲存庫克隆到「主目錄」中的 <code>Homestead</code> 資料夾中，因為 Homestead 虛擬機將作為所有 Laravel 應用程式的主機。在整個文檔中，我們將此目錄稱為「Homestead 目錄」：</p>
<pre><code class="language-shell">git clone https://github.com/laravel/homestead.git ~/Homestead
</code></pre>
<p>克隆 Laravel Homestead 儲存庫後，您應該切換到 <code>release</code> 分支。此分支始終包含 Homestead 的最新穩定版本：</p>
<pre><code class="language-shell">cd ~/Homestead

git checkout release
</code></pre>
<p>接下來，從 Homestead 目錄執行 <code>bash init.sh</code> 命令來建立 <code>Homestead.yaml</code> 配置檔案。<code>Homestead.yaml</code> 檔案是您配置所有 Homestead 安裝設定的地方。此檔案將放置在 Homestead 目錄中：</p>
<pre><code class="language-shell"># macOS / Linux...
bash init.sh

# Windows...
init.bat
</code></pre>
<p><a name="configuring-homestead"></a></p>
<h3>配置 Homestead</h3>
<p><a name="setting-your-provider"></a></p>
<h4>設定您的供應商</h4>
<p><code>Homestead.yaml</code> 檔案中的 <code>provider</code> 鍵表示應使用哪個 Vagrant 供應商：<code>virtualbox</code> 或 <code>parallels</code>：</p>
<pre><code>provider: virtualbox
</code></pre>
<blockquote>
<p>[!WARNING]
如果您使用 Apple Silicon，則需要 Parallels 供應商。</p>
</blockquote>
<p><a name="configuring-shared-folders"></a></p>
<h4>配置共享資料夾</h4>
<p><code>Homestead.yaml</code> 檔案的 <code>folders</code> 屬性列出了您希望與 Homestead 環境共享的所有資料夾。當這些資料夾中的檔案發生更改時，它們將在您的本機和 Homestead 虛擬環境之間保持同步。您可以根據需要配置任意數量的共享資料夾：</p>
<pre><code class="language-yaml">folders:
    - map: ~/code/project1
      to: /home/vagrant/project1
</code></pre>
<blockquote>
<p>[!WARNING]
Windows 使用者不應使用 <code>~/</code> 路徑語法，而應使用專案的完整路徑，如 <code>C:\\Users\\user\\Code\\project1</code>。</p>
</blockquote>
<p>您應該始終將單個應用程式映射到自己的資料夾映射，而不是映射包含所有應用程式的單個大目錄。當您映射資料夾時，虛擬機器必須追蹤資料夾中<em>每個</em>檔案的所有磁碟 IO。如果資料夾中有大量檔案，您可能會體驗到效能降低：</p>
<pre><code class="language-yaml">folders:
    - map: ~/code/project1
      to: /home/vagrant/project1
    - map: ~/code/project2
      to: /home/vagrant/project2
</code></pre>
<blockquote>
<p>[!WARNING]
使用 Homestead 時，您不應掛載 <code>.</code>（當前目錄）。這會導致 Vagrant 不將當前資料夾映射到 <code>/vagrant</code>，並會破壞可選功能並在供應期間導致意外結果。</p>
</blockquote>
<p>要啟用 <a href="https://developer.hashicorp.com/vagrant/docs/synced-folders/nfs">NFS</a>，您可以向資料夾映射添加 <code>type</code> 選項：</p>
<pre><code class="language-yaml">folders:
    - map: ~/code/project1
      to: /home/vagrant/project1
      type: &quot;nfs&quot;
</code></pre>
<blockquote>
<p>[!WARNING]
在 Windows 上使用 NFS 時，您應該考慮安裝 <a href="https://github.com/winnfsd/vagrant-winnfsd">vagrant-winnfsd</a> 外掛。此將保持 Homestead 虛擬機內檔案和目錄的正確使用者/群組權限。</p>
</blockquote>
<p>您還可以透過在 <code>options</code> 鍵下列出它們來傳遞 Vagrant <a href="https://developer.hashicorp.com/vagrant/docs/synced-folders/basic_usage">Synced Folders</a> 支援的任何選項：</p>
<pre><code class="language-yaml">folders:
    - map: ~/code/project1
      to: /home/vagrant/project1
      type: &quot;rsync&quot;
      options:
          rsync__args: [&quot;--verbose&quot;, &quot;--archive&quot;, &quot;--delete&quot;, &quot;-zz&quot;]
          rsync__exclude: [&quot;node_modules&quot;]
</code></pre>
<p><a name="configuring-nginx-sites"></a></p>
<h3>配置 Nginx 站點</h3>
<p>不熟悉 Nginx？沒問題。您的 <code>Homestead.yaml</code> 檔案的 <code>sites</code> 屬性允許您輕鬆地將「網域」映射到 Homestead 環境中的資料夾。範例站點配置包含在 <code>Homestead.yaml</code> 檔案中。同樣，您可以根據需要向 Homestead 環境添加任意數量的站點。Homestead 可以作為您正在處理的每個 Laravel 應用程式的便利虛擬化環境：</p>
<pre><code class="language-yaml">sites:
    - map: homestead.test
      to: /home/vagrant/project1/public
</code></pre>
<p>如果您在供應 Homestead 虛擬機後更改了 <code>sites</code> 屬性，您應該在終端中執行 <code>vagrant reload --provision</code> 命令來更新虛擬機器上的 Nginx 配置。</p>
<blockquote>
<p>[!WARNING]
Homestead 腳本的建立目標是盡可能保持幂等性。但是，如果您在供應期間遇到問題，您應該透過執行 <code>vagrant destroy &amp;&amp; vagrant up</code> 命令來銷毀並重建機器。</p>
</blockquote>
<p><a name="hostname-resolution"></a></p>
<h4>主機名解析</h4>
<p>Homestead 使用 <code>mDNS</code> 發布主機名以實現自動主機解析。如果您在 <code>Homestead.yaml</code> 檔案中設定 <code>hostname: homestead</code>，主機將在 <code>homestead.local</code> 上可用。macOS、iOS 和 Linux 桌面發行版預設包含 <code>mDNS</code> 支援。如果您使用 Windows，您必須安裝 <a href="https://support.apple.com/kb/DL999?viewlocale=en_US&locale=en_US">Bonjour Print Services for Windows</a>。</p>
<p>使用自動主機名最適合 Homestead 的<a href="#per-project-installation">按專案安裝</a>。如果您在單個 Homestead 實例上託管多個站點，您可以將站點的「網域」添加到您機器上的 <code>hosts</code> 檔案中。<code>hosts</code> 檔案將把對 Homestead 站點的請求重定向到您的 Homestead 虛擬機器。在 macOS 和 Linux 上，此檔案位於 <code>/etc/hosts</code>。在 Windows 上，它位於 <code>C:\\Windows\\System32\\drivers\\etc\\hosts</code>。您添加到此檔案的行如下所示：</p>
<pre><code class="language-text">192.168.56.56  homestead.test
</code></pre>
<p>確保列出的 IP 地址是在您的 <code>Homestead.yaml</code> 檔案中設定的地址。一旦您將網域添加到 <code>hosts</code> 檔案並啟動了 Vagrant box，您就可以透過 Web 瀏覽器存取該站點：</p>
<pre><code class="language-shell">http://homestead.test
</code></pre>
<p><a name="configuring-services"></a></p>
<h3>配置服務</h3>
<p>Homestead 預設啟動幾個服務；但是，您可以自訂在供應期間啟用或禁用哪些服務。例如，您可以透過修改 <code>Homestead.yaml</code> 檔案中的 <code>services</code> 選項來啟用 PostgreSQL 並禁用 MySQL：</p>
<pre><code class="language-yaml">services:
    - enabled:
        - &quot;postgresql&quot;
    - disabled:
        - &quot;mysql&quot;
</code></pre>
<p>指定的服務將根據其在 <code>enabled</code> 和 <code>disabled</code> 指令中的順序來啟動或停止。</p>
<p><a name="launching-the-vagrant-box"></a></p>
<h3>啟動 Vagrant Box</h3>
<p>一旦您按照自己的喜好編輯了 <code>Homestead.yaml</code>，請從您的 Homestead 目錄運行 <code>vagrant up</code> 命令。Vagrant 將啟動虛擬機器並自動配置您的共享資料夾和 Nginx 站點。</p>
<p>要銷毀機器，您可以使用 <code>vagrant destroy</code> 命令。</p>
<p><a name="per-project-installation"></a></p>
<h3>按專案安裝</h3>
<p>除了全域安裝 Homestead 並在所有專案之間共享同一個 Homestead 虛擬機器之外，您可以為管理的每個專案配置一個 Homestead 實例。如果您希望將 <code>Vagrantfile</code> 與專案一起分發，允許其他處理該專案的人在克隆專案儲存庫後立即 <code>vagrant up</code>，那麼按專案安裝 Homestead 可能是有益的。</p>
<p>您可以使用 Composer 套件管理器將 Homestead 安裝到您的專案中：</p>
<pre><code class="language-shell">composer require laravel/homestead --dev
</code></pre>
<p>安裝 Homestead 後，調用 Homestead 的 <code>make</code> 命令為您的專案生成 <code>Vagrantfile</code> 和 <code>Homestead.yaml</code> 檔案。這些檔案將放置在專案的根目錄中。<code>make</code> 命令將自動配置 <code>Homestead.yaml</code> 檔案中的 <code>sites</code> 和 <code>folders</code> 指令：</p>
<pre><code class="language-shell"># macOS / Linux...
php vendor/bin/homestead make

# Windows...
vendor\\\\bin\\\\homestead make
</code></pre>
<p>接下來，在終端中運行 <code>vagrant up</code> 命令，並在瀏覽器中透過 <code>http://homestead.test</code> 存取您的專案。請記住，如果您不使用自動<a href="#hostname-resolution">主機名解析</a>，您仍然需要為 <code>homestead.test</code> 或您選擇的網域添加 <code>/etc/hosts</code> 檔案條目。</p>
<p><a name="installing-optional-features"></a></p>
<h3>安裝可選功能</h3>
<p>可選軟體使用 <code>Homestead.yaml</code> 檔案中的 <code>features</code> 選項安裝。大多數功能可以用布林值啟用或禁用，而有些功能允許多個配置選項：</p>
<pre><code class="language-yaml">features:
    - blackfire:
        server_id: &quot;server_id&quot;
        server_token: &quot;server_value&quot;
        client_id: &quot;client_id&quot;
        client_token: &quot;client_value&quot;
    - cassandra: true
    - chronograf: true
    - couchdb: true
    - crystal: true
    - dragonflydb: true
    - elasticsearch:
        version: 7.9.0
    - eventstore: true
        version: 21.2.0
    - flyway: true
    - gearman: true
    - golang: true
    - grafana: true
    - influxdb: true
    - logstash: true
    - mariadb: true
    - meilisearch: true
    - minio: true
    - mongodb: true
    - neo4j: true
    - ohmyzsh: true
    - openresty: true
    - pm2: true
    - python: true
    - r-base: true
    - rabbitmq: true
    - rustc: true
    - rvm: true
    - solr: true
    - timescaledb: true
    - trader: true
    - webdriver: true
</code></pre>
<p><a name="elasticsearch"></a></p>
<h4>Elasticsearch</h4>
<p>您可以指定支援的 Elasticsearch 版本，它必須是確切的版本號（major.minor.patch）。預設安裝將建立一個名為 'homestead' 的叢集。您不應給予 Elasticsearch 超過作業系統記憶體一半的空間，因此請確保您的 Homestead 虛擬機器至少有 Elasticsearch 分配量的兩倍。</p>
<blockquote>
<p>[!NOTE]
請查看 <a href="https://www.elastic.co/guide/en/elasticsearch/reference/current">Elasticsearch 文檔</a>以了解如何自訂您的配置。</p>
</blockquote>
<p><a name="mariadb"></a></p>
<h4>MariaDB</h4>
<p>啟用 MariaDB 將移除 MySQL 並安裝 MariaDB。MariaDB 通常作為 MySQL 的直接替代品，因此您仍應在應用程式的資料庫配置中使用 <code>mysql</code> 資料庫驅動程式。</p>
<p><a name="mongodb"></a></p>
<h4>MongoDB</h4>
<p>預設的 MongoDB 安裝將資料庫使用者名設定為 <code>homestead</code>，相應的密碼設定為 <code>secret</code>。</p>
<p><a name="neo4j"></a></p>
<h4>Neo4j</h4>
<p>預設的 Neo4j 安裝將資料庫使用者名設定為 <code>homestead</code>，相應的密碼設定為 <code>secret</code>。要存取 Neo4j 瀏覽器，請透過您的 Web 瀏覽器訪問 <code>http://homestead.test:7474</code>。端口 <code>7687</code>（Bolt）、<code>7474</code>（HTTP）和 <code>7473</code>（HTTPS）已準備好為來自 Neo4j 客戶端的請求提供服務。</p>
<p><a name="aliases"></a></p>
<h3>別名</h3>
<p>您可以透過修改 Homestead 目錄中的 <code>aliases</code> 檔案向 Homestead 虛擬機器添加 Bash 別名：</p>
<pre><code class="language-shell">alias c=&#39;clear&#39;
alias ..=&#39;cd ..&#39;
</code></pre>
<p>更新 <code>aliases</code> 檔案後，您應該使用 <code>vagrant reload --provision</code> 命令重新供應 Homestead 虛擬機器。這將確保您的新別名在機器上可用。</p>
<p><a name="updating-homestead"></a></p>
<h2>更新 Homestead</h2>
<p>開始更新 Homestead 之前，您應該確保已透過在 Homestead 目錄中運行以下命令來移除當前虛擬機器：</p>
<pre><code class="language-shell">vagrant destroy
</code></pre>
<p>接下來，您需要更新 Homestead 原始碼。如果您克隆了儲存庫，可以在您最初克隆儲存庫的位置執行以下命令：</p>
<pre><code class="language-shell">git fetch

git pull origin release
</code></pre>
<p>這些命令從 GitHub 儲存庫拉取最新的 Homestead 程式碼，獲取最新的標籤，然後切換到最新的標籤發布。您可以在 Homestead 的 <a href="https://github.com/laravel/homestead/releases">GitHub 發佈頁面</a>上找到最新的穩定版本。</p>
<p>如果您透過專案的 <code>composer.json</code> 檔案安裝了 Homestead，您應該確保 <code>composer.json</code> 檔案包含 <code>"laravel/homestead": "^12"</code> 並更新您的依賴項：</p>
<pre><code class="language-shell">composer update
</code></pre>
<p>接下來，您應該使用 <code>vagrant box update</code> 命令更新 Vagrant box：</p>
<pre><code class="language-shell">vagrant box update
</code></pre>
<p>更新 Vagrant box 後，您應該從 Homestead 目錄運行 <code>bash init.sh</code> 命令來更新 Homestead 的其他配置檔案。系統會詢問您是否要覆蓋現有的 <code>Homestead.yaml</code>、<code>after.sh</code> 和 <code>aliases</code> 檔案：</p>
<pre><code class="language-shell"># macOS / Linux...
bash init.sh

# Windows...
init.bat
</code></pre>
<p>最後，您需要重新生成 Homestead 虛擬機器以使用最新的 Vagrant 安裝：</p>
<pre><code class="language-shell">vagrant up
</code></pre>
<p><a name="daily-usage"></a></p>
<h2>日常使用</h2>
<p><a name="connecting-via-ssh"></a></p>
<h3>透過 SSH 連接</h3>
<p>您可以透過從 Homestead 目錄執行 <code>vagrant ssh</code> 終端命令來 SSH 進入您的虛擬機器。</p>
<p><a name="adding-additional-sites"></a></p>
<h3>添加其他站點</h3>
<p>一旦您的 Homestead 環境被供應並運行，您可能想為您的其他 Laravel 專案添加其他 Nginx 站點。您可以在單個 Homestead 環境上運行任意數量的 Laravel 專案。要添加其他站點，請將站點添加到您的 <code>Homestead.yaml</code> 檔案中。</p>
<pre><code class="language-yaml">sites:
    - map: homestead.test
      to: /home/vagrant/project1/public
    - map: another.test
      to: /home/vagrant/project2/public
</code></pre>
<blockquote>
<p>[!WARNING]
在添加站點之前，您應該確保已為專案的目錄配置了<a href="#configuring-shared-folders">資料夾映射</a>。</p>
</blockquote>
<p>如果 Vagrant 沒有自動管理您的「hosts」檔案，您可能還需要將新站點添加到該檔案中。在 macOS 和 Linux 上，此檔案位於 <code>/etc/hosts</code>。在 Windows 上，它位於 <code>C:\\Windows\\System32\\drivers\\etc\\hosts</code>：</p>
<pre><code class="language-text">192.168.56.56  homestead.test
192.168.56.56  another.test
</code></pre>
<p>添加站點後，請從 Homestead 目錄執行 <code>vagrant reload --provision</code> 終端命令。</p>
<p><a name="site-types"></a></p>
<h4>站點類型</h4>
<p>Homestead 支援幾種「類型」的站點，允許您輕鬆運行不基於 Laravel 的專案。例如，我們可以使用 <code>statamic</code> 站點類型輕鬆地將 Statamic 應用程式添加到 Homestead：</p>
<pre><code class="language-yaml">sites:
    - map: statamic.test
      to: /home/vagrant/my-symfony-project/web
      type: &quot;statamic&quot;
</code></pre>
<p>可用的站點類型有：<code>apache</code>、<code>apache-proxy</code>、<code>apigility</code>、<code>expressive</code>、<code>laravel</code>（預設）、<code>proxy</code>（用於 nginx）、<code>silverstripe</code>、<code>statamic</code>、<code>symfony2</code>、<code>symfony4</code> 和 <code>zf</code>。</p>
<p><a name="site-parameters"></a></p>
<h4>站點參數</h4>
<p>您可以透過 <code>params</code> 站點指令向您的站點添加額外的 Nginx <code>fastcgi_param</code> 值：</p>
<pre><code class="language-yaml">sites:
    - map: homestead.test
      to: /home/vagrant/project1/public
      params:
          - key: FOO
            value: BAR
</code></pre>
<p><a name="environment-variables"></a></p>
<h3>環境變數</h3>
<p>您可以透過將它們添加到 <code>Homestead.yaml</code> 檔案中來定義全域環境變數：</p>
<pre><code class="language-yaml">variables:
    - key: APP_ENV
      value: local
    - key: FOO
      value: bar
</code></pre>
<p>更新 <code>Homestead.yaml</code> 檔案後，請確保透過執行 <code>vagrant reload --provision</code> 命令重新供應機器。這將更新所有已安裝 PHP 版本的 PHP-FPM 配置，並更新 <code>vagrant</code> 使用者的環境。</p>
<p><a name="ports"></a></p>
<h3>端口</h3>
<p>預設情況下，以下端口被轉發到您的 Homestead 環境：</p>
<div class="content-list" markdown="1"><ul>
<li><strong>HTTP：</strong> 8000 &rarr; 轉發到 80</li>
<li><strong>HTTPS：</strong> 44300 &rarr; 轉發到 443</li>
</ul>
</div><p><a name="forwarding-additional-ports"></a></p>
<h4>轉發其他端口</h4>
<p>如果您願意，可以透過在 <code>Homestead.yaml</code> 檔案中定義 <code>ports</code> 配置條目來向 Vagrant box 轉發其他端口。更新 <code>Homestead.yaml</code> 檔案後，請確保透過執行 <code>vagrant reload --provision</code> 命令重新供應機器：</p>
<pre><code class="language-yaml">ports:
    - send: 50000
      to: 5000
    - send: 7777
      to: 777
      protocol: udp
</code></pre>
<p>以下是您可能希望從主機映射到 Vagrant box 的其他 Homestead 服務端口列表：</p>
<div class="content-list" markdown="1"><ul>
<li><strong>SSH：</strong> 2222 &rarr; 到 22</li>
<li><strong>ngrok UI：</strong> 4040 &rarr; 到 4040</li>
<li><strong>MySQL：</strong> 33060 &rarr; 到 3306</li>
<li><strong>PostgreSQL：</strong> 54320 &rarr; 到 5432</li>
<li><strong>MongoDB：</strong> 27017 &rarr; 到 27017</li>
<li><strong>Mailpit：</strong> 8025 &rarr; 到 8025</li>
<li><strong>Minio：</strong> 9600 &rarr; 到 9600</li>
</ul>
</div><p><a name="php-versions"></a></p>
<h3>PHP 版本</h3>
<p>Homestead 支援在同一個虛擬機器上運行多個版本的 PHP。您可以在 <code>Homestead.yaml</code> 檔案中指定給定站點使用哪個版本的 PHP。可用的 PHP 版本有：「5.6」、「7.0」、「7.1」、「7.2」、「7.3」、「7.4」、「8.0」、「8.1」、「8.2」和「8.3」（預設）：</p>
<pre><code class="language-yaml">sites:
    - map: homestead.test
      to: /home/vagrant/project1/public
      php: &quot;7.1&quot;
</code></pre>
<p><a href="#connecting-via-ssh">在您的 Homestead 虛擬機器內</a>，您可以透過 CLI 使用任何支援的 PHP 版本：</p>
<pre><code class="language-shell">php5.6 artisan list
php7.0 artisan list
php7.1 artisan list
php7.2 artisan list
php7.3 artisan list
php7.4 artisan list
php8.0 artisan list
php8.1 artisan list
php8.2 artisan list
php8.3 artisan list
</code></pre>
<p>您可以透過在 Homestead 虛擬機器內發出以下命令來更改 CLI 使用的預設 PHP 版本：</p>
<pre><code class="language-shell">php56
php70
php71
php72
php73
php74
php80
php81
php82
php83
</code></pre>
<p><a name="connecting-to-databases"></a></p>
<h3>連接資料庫</h3>
<p>MySQL 和 PostgreSQL 都預設配置了一個 <code>homestead</code> 資料庫。要從主機的資料庫客戶端連接到您的 MySQL 或 PostgreSQL 資料庫，您應該連接到 <code>127.0.0.1</code> 的端口 <code>33060</code>（MySQL）或 <code>54320</code>（PostgreSQL）。兩個資料庫的使用者名和密碼都是 <code>homestead</code> / <code>secret</code>。</p>
<blockquote>
<p>[!WARNING]
您只應在從主機連接資料庫時使用這些非標準端口。您將在 Laravel 應用程式的 <code>database</code> 配置檔案中使用預設的 3306 和 5432 端口，因為 Laravel 運行在<em>虛擬機器內部</em>。</p>
</blockquote>
<p><a name="database-backups"></a></p>
<h3>資料庫備份</h3>
<p>Homestead 可以在 Homestead 虛擬機器被銷毀時自動備份您的資料庫。要使用此功能，您必須使用 Vagrant 2.1.0 或更高版本。或者，如果您使用的是較舊版本的 Vagrant，您必須安裝 <code>vagrant-triggers</code> 外掛。要啟用自動資料庫備份，請將以下行添加到您的 <code>Homestead.yaml</code> 檔案中：</p>
<pre><code class="language-yaml">backup: true
</code></pre>
<p>配置後，當執行 <code>vagrant destroy</code> 命令時，Homestead 將您的資料庫匯出到 <code>.backup/mysql_backup</code> 和 <code>.backup/postgres_backup</code> 目錄。如果您使用<a href="#per-project-installation">按專案安裝</a>方法，這些目錄可以在您安裝 Homestead 的資料夾中或專案的根目錄中找到。</p>
<p><a name="configuring-cron-schedules"></a></p>
<h3>配置 Cron 排程</h3>
<p>Laravel 提供了一種便利的方式來<a href="/docs/scheduling">排程 cron 任務</a>，方法是排程單個 <code>schedule:run</code> Artisan 命令每分鐘運行一次。<code>schedule:run</code> 命令將檢查在 <code>routes/console.php</code> 檔案中定義的任務排程，以確定要運行哪些已排程的任務。</p>
<p>如果您希望 <code>schedule:run</code> 命令為 Homestead 站點運行，您可以在定義站點時將 <code>schedule</code> 選項設定為 <code>true</code>：</p>
<pre><code class="language-yaml">sites:
    - map: homestead.test
      to: /home/vagrant/project1/public
      schedule: true
</code></pre>
<p>站點的 cron 任務將定義在 Homestead 虛擬機器的 <code>/etc/cron.d</code> 目錄中。</p>
<p><a name="configuring-mailpit"></a></p>
<h3>配置 Mailpit</h3>
<p><a href="https://github.com/axllent/mailpit">Mailpit</a> 允許您攔截發出的電子郵件並在實際將郵件發送給收件者之前對其進行檢查。首先，更新應用程式的 <code>.env</code> 檔案以使用以下郵件設定：</p>
<pre><code class="language-ini">MAIL_MAILER=smtp
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
</code></pre>
<p>配置 Mailpit 後，您可以在 <code>http://localhost:8025</code> 存取 Mailpit 儀表板。</p>
<p><a name="configuring-minio"></a></p>
<h3>配置 Minio</h3>
<p><a href="https://github.com/minio/minio">Minio</a> 是一個開源物件儲存伺服器，具有 Amazon S3 相容的 API。要安裝 Minio，請使用 <a href="#installing-optional-features">features</a> 部分中的以下配置選項更新您的 <code>Homestead.yaml</code> 檔案：</p>
<pre><code>minio: true
</code></pre>
<p>預設情況下，Minio 在端口 9600 上可用。您可以透過訪問 <code>http://localhost:9600</code> 來存取 Minio 控制面板。預設存取金鑰是 <code>homestead</code>，而預設的秘密金鑰是 <code>secretkey</code>。存取 Minio 時，您應始終使用區域 <code>us-east-1</code>。</p>
<p>要使用 Minio，請確保您的 <code>.env</code> 檔案具有以下選項：</p>
<pre><code class="language-ini">AWS_USE_PATH_STYLE_ENDPOINT=true
AWS_ENDPOINT=http://localhost:9600
AWS_ACCESS_KEY_ID=homestead
AWS_SECRET_ACCESS_KEY=secretkey
AWS_DEFAULT_REGION=us-east-1
</code></pre>
<p>要供應 Minio 驅動的「S3」儲存桶，請向您的 <code>Homestead.yaml</code> 檔案添加 <code>buckets</code> 指令。定義儲存桶後，您應該在終端中執行 <code>vagrant reload --provision</code> 命令：</p>
<pre><code class="language-yaml">buckets:
    - name: your-bucket
      policy: public
    - name: your-private-bucket
      policy: none
</code></pre>
<p>支援的 <code>policy</code> 值包括：<code>none</code>、<code>download</code>、<code>upload</code> 和 <code>public</code>。</p>
<p><a name="laravel-dusk"></a></p>
<h3>Laravel Dusk</h3>
<p>要在 Homestead 中運行 <a href="/docs/dusk">Laravel Dusk</a> 測試，您應該在 Homestead 配置中啟用 <a href="#installing-optional-features">webdriver 功能</a>：</p>
<pre><code class="language-yaml">features:
    - webdriver: true
</code></pre>
<p>啟用 <code>webdriver</code> 功能後，您應該在終端中執行 <code>vagrant reload --provision</code> 命令。</p>
<p><a name="sharing-your-environment"></a></p>
<h3>共享您的環境</h3>
<p>有時您可能想與同事或客戶分享您目前正在處理的內容。Vagrant 透過 <code>vagrant share</code> 命令內建支援此功能；但是，如果您在 <code>Homestead.yaml</code> 檔案中配置了多個站點，這將不起作用。</p>
<p>要解決此問題，Homestead 包含其自己的 <code>share</code> 命令。首先，透過 <code>vagrant ssh</code> <a href="#connecting-via-ssh">SSH 進入您的 Homestead 虛擬機器</a>並執行 <code>share homestead.test</code> 命令。此命令將共享 <code>Homestead.yaml</code> 配置檔案中的 <code>homestead.test</code> 站點。您可以將任何其他已配置的站點替換為 <code>homestead.test</code>：</p>
<pre><code class="language-shell">share homestead.test
</code></pre>
<p>運行命令後，您將看到一個 Ngrok 螢幕出現，其中包含活動日誌和共享站點的可公開存取的 URL。如果您想指定自訂區域、子網域或其他 Ngrok 運行時選項，您可以將它們添加到 <code>share</code> 命令中：</p>
<pre><code class="language-shell">share homestead.test -region=eu -subdomain=laravel
</code></pre>
<p>如果您需要透過 HTTPS 而不是 HTTP 共享內容，使用 <code>sshare</code> 命令而不是 <code>share</code> 將使您能夠做到這一點。</p>
<blockquote>
<p>[!WARNING]
請記住，Vagrant 本質上是不安全的，當您運行 <code>share</code> 命令時，您正在將虛擬機器暴露在 Internet 上。</p>
</blockquote>
<p><a name="debugging-and-profiling"></a></p>
<h2>除錯和效能分析</h2>
<p><a name="debugging-web-requests"></a></p>
<h3>使用 Xdebug 除錯 Web 請求</h3>
<p>Homestead 包含使用 <a href="https://xdebug.org">Xdebug</a> 進行單步除錯的支援。例如，您可以存取瀏覽器中的頁面，PHP 將連接到您的 IDE 以允許檢查和修改正在運行的程式碼。</p>
<p>預設情況下，Xdebug 已經運行並準備好接受連接。如果您需要在 CLI 上啟用 Xdebug，請在 Homestead 虛擬機器內執行 <code>sudo phpenmod xdebug</code> 命令。接下來，按照 IDE 的說明啟用除錯。最後，配置瀏覽器以使用擴充套件或<a href="https://www.jetbrains.com/phpstorm/marklets/">書籤列</a>觸發 Xdebug。</p>
<blockquote>
<p>[!WARNING]
Xdebug 會導致 PHP 運行顯著變慢。要禁用 Xdebug，請在 Homestead 虛擬機器內運行 <code>sudo phpdismod xdebug</code> 並重新啟動 FPM 服務。</p>
</blockquote>
<p><a name="autostarting-xdebug"></a></p>
<h4>自動啟動 Xdebug</h4>
<p>除錯向 Web 伺服器發出請求的功能測試時，自動啟動除錯比修改測試以透過自訂標頭或 Cookie 觸發除錯更容易。要強制 Xdebug 自動開始，請修改 Homestead 虛擬機器內的 <code>/etc/php/7.x/fpm/conf.d/20-xdebug.ini</code> 檔案並添加以下配置：</p>
<pre><code class="language-ini">; 如果 Homestead.yaml 包含 IP 地址的不同子網，此地址可能不同...
xdebug.client_host = 192.168.10.1
xdebug.mode = debug
xdebug.start_with_request = yes
</code></pre>
<p><a name="debugging-cli-applications"></a></p>
<h3>除錯 CLI 應用程式</h3>
<p>要除錯 PHP CLI 應用程式，請在 Homestead 虛擬機器內使用 <code>xphp</code> shell 別名：</p>
<pre><code class="language-shell">xphp /path/to/script
</code></pre>
<p><a name="profiling-applications-with-blackfire"></a></p>
<h3>使用 Blackfire 進行效能分析</h3>
<p><a href="https://blackfire.io/docs/introduction">Blackfire</a> 是一個用於分析 Web 請求和 CLI 應用程式的服務。它提供了一個互動式使用者介面，在呼叫圖和時間軸中顯示分析資料。它專為在開發、預備和生產環境中使用而建立，對最終使用者沒有開銷。此外，Blackfire 還對程式碼和 <code>php.ini</code> 配置設定提供效能、品質和安全檢查。</p>
<p><a href="https://blackfire.io/docs/player/index">Blackfire Player</a> 是一個開源的 Web 爬蟲、Web 測試和 Web 抓取應用程式，可以與 Blackfire 聯合使用來編寫分析場景的腳本。</p>
<p>要啟用 Blackfire，請使用 Homestead 配置檔案中的「features」設定：</p>
<pre><code class="language-yaml">features:
    - blackfire:
        server_id: &quot;server_id&quot;
        server_token: &quot;server_value&quot;
        client_id: &quot;client_id&quot;
        client_token: &quot;client_value&quot;
</code></pre>
<p>Blackfire 伺服器憑證和客戶端憑證<a href="https://blackfire.io/signup">需要 Blackfire 帳戶</a>。Blackfire 提供了多種選項來分析應用程式，包括 CLI 工具和瀏覽器擴充套件。請<a href="https://blackfire.io/docs/php/integrations/laravel/index">查看 Blackfire 文檔以了解更多詳情</a>。</p>
<p><a name="network-interfaces"></a></p>
<h2>網路介面</h2>
<p><code>Homestead.yaml</code> 檔案的 <code>networks</code> 屬性配置 Homestead 虛擬機器的網路介面。您可以根據需要配置任意數量的介面：</p>
<pre><code class="language-yaml">networks:
    - type: &quot;private_network&quot;
      ip: &quot;192.168.10.20&quot;
</code></pre>
<p>要啟用<a href="https://developer.hashicorp.com/vagrant/docs/networking/public_network">橋接</a>介面，請為網路配置 <code>bridge</code> 設定並將網路類型更改為 <code>public_network</code>：</p>
<pre><code class="language-yaml">networks:
    - type: &quot;public_network&quot;
      ip: &quot;192.168.10.20&quot;
      bridge: &quot;en1: Wi-Fi (AirPort)&quot;
</code></pre>
<p>要啟用 <a href="https://developer.hashicorp.com/vagrant/docs/networking/public_network#dhcp">DHCP</a>，只需從配置中移除 <code>ip</code> 選項：</p>
<pre><code class="language-yaml">networks:
    - type: &quot;public_network&quot;
      bridge: &quot;en1: Wi-Fi (AirPort)&quot;
</code></pre>
<p>要更新網路正在使用的設備，您可以向網路的配置添加 <code>dev</code> 選項。預設的 <code>dev</code> 值是 <code>eth0</code>：</p>
<pre><code class="language-yaml">networks:
    - type: &quot;public_network&quot;
      ip: &quot;192.168.10.20&quot;
      bridge: &quot;en1: Wi-Fi (AirPort)&quot;
      dev: &quot;enp2s0&quot;
</code></pre>
<p><a name="extending-homestead"></a></p>
<h2>擴展 Homestead</h2>
<p>您可以使用 Homestead 目錄根目錄中的 <code>after.sh</code> 腳本來擴展 Homestead。在此檔案中，您可以添加正確配置和自訂虛擬機器所需的任何 shell 命令。</p>
<p>自訂 Homestead 時，Ubuntu 可能會詢問您是否要保留套件的原始配置或使用新配置檔案覆蓋它。為了避免這種情況，您在安裝套件時應使用以下命令以避免覆蓋 Homestead 先前寫入的任何配置：</p>
<pre><code class="language-shell">sudo apt-get -y \\
    -o Dpkg::Options::=&quot;--force-confdef&quot; \\
    -o Dpkg::Options::=&quot;--force-confold&quot; \\
    install package-name
</code></pre>
<p><a name="user-customizations"></a></p>
<h3>使用者自訂</h3>
<p>與團隊一起使用 Homestead 時，您可能想要調整 Homestead 以更好地適應您的個人開發風格。為了實現這一點，您可以在 Homestead 目錄的根目錄（包含 <code>Homestead.yaml</code> 檔案的同一目錄）中建立一個 <code>user-customizations.sh</code> 檔案。在此檔案中，您可以進行任何您想要的自訂；但是，<code>user-customizations.sh</code> 不應被版本控制。</p>
<p><a name="provider-specific-settings"></a></p>
<h2>供應商特定設定</h2>
<p><a name="provider-specific-virtualbox"></a></p>
<h3>VirtualBox</h3>
<p><a name="natdnshostresolver"></a></p>
<h4><code>natdnshostresolver</code></h4>
<p>預設情況下，Homestead 將 <code>natdnshostresolver</code> 設定配置為 <code>on</code>。這允許 Homestead 使用您的主機作業系統的 DNS 設定。如果您想覆蓋此行為，請向您的 <code>Homestead.yaml</code> 檔案添加以下配置選項：</p>
<pre><code class="language-yaml">provider: virtualbox
natdnshostresolver: &#39;off&#39;
</code></pre>
`;
---

<DocsLayout title="Homestead" currentPath="homestead">
  <div class="mb-8">
    <nav class="text-sm text-text-secondary mb-4">
      <a href="/" class="hover:text-accent">首頁</a>
      <span class="mx-2">/</span>
      <span>文件</span>
      <span class="mx-2">/</span>
      <span>Homestead</span>
    </nav>
    
    <h1 class="text-4xl font-bold mb-4">Homestead</h1>
    
    <div class="bg-bg-secondary border border-border rounded-lg p-4 mb-6">
      <p class="text-text-secondary">
        📝 此頁面為 Laravel 官方文檔的繁體中文翻譯。
        <a href="https://laravel.com/docs/13.x/homestead" class="text-accent" target="_blank" rel="noopener">
          查看原始英文版本
        </a>
      </p>
    </div>
  </div>

  <div class="prose prose-invert max-w-none" set:html={content}></div>
</DocsLayout>
