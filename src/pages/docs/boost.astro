---
import DocsLayout from '../../layouts/DocsLayout.astro';

const content = `<h1>Laravel Boost</h1>
<ul>
<li><a href="#introduction">簡介</a></li>
<li><a href="#installation">安裝</a><ul>
<li><a href="#set-up-your-agents">設定您的代理</a></li>
<li><a href="#keeping-boost-resources-updated">保持 Boost 資源更新</a></li>
</ul>
</li>
<li><a href="#mcp-server">MCP 伺服器</a><ul>
<li><a href="#available-mcp-tools">可用的 MCP 工具</a></li>
<li><a href="#manually-registering-the-mcp-server">手動註冊 MCP 伺服器</a></li>
</ul>
</li>
<li><a href="#ai-guidelines">AI 指南</a><ul>
<li><a href="#available-ai-guidelines">可用的 AI 指南</a></li>
<li><a href="#adding-custom-ai-guidelines">添加自訂 AI 指南</a></li>
<li><a href="#overriding-boost-ai-guidelines">覆蓋 Boost AI 指南</a></li>
<li><a href="#third-party-package-ai-guidelines">第三方套件 AI 指南</a></li>
</ul>
</li>
<li><a href="#agent-skills">代理技能</a><ul>
<li><a href="#available-skills">可用技能</a></li>
<li><a href="#custom-skills">自訂技能</a></li>
<li><a href="#overriding-skills">覆蓋技能</a></li>
<li><a href="#third-party-package-skills">第三方套件技能</a></li>
</ul>
</li>
<li><a href="#guidelines-vs-skills">指南 vs. 技能</a></li>
<li><a href="#documentation-api">文件 API</a></li>
<li><a href="#extending-boost">擴展 Boost</a><ul>
<li><a href="#adding-support-for-other-ides-ai-agents">添加對其他 IDE / AI 代理的支援</a></li>
</ul>
</li>
</ul>
<p><a name="introduction"></a></p>
<h2>簡介</h2>
<p>Laravel Boost 透過提供 essential 指南和代理技能來加速 AI 輔助開發，這些指南和技能幫助 AI 代理編寫高品質的 Laravel 應用程式，並遵循 Laravel 的最佳實踐。</p>
<p>Boost 還提供了一個強大的 Laravel 生態系統文件 API，該 API 結合了內建的 MCP 工具和包含超過 17,000 條 Laravel 特定資訊的廣泛知識庫，所有這些都透過使用嵌入向量的語義搜尋功能進行了增強，以實現精確、上下文感知的結果。Boost 指導像 Claude Code 和 Cursor 這樣的 AI 代理使用此 API 來學習最新的 Laravel 功能和最佳實踐。</p>
<p><a name="installation"></a></p>
<h2>安裝</h2>
<p>Laravel Boost 可以透過 Composer 安裝：</p>
<pre><code class="language-shell">composer require laravel/boost --dev
</code></pre>
<p>接下來，安裝 MCP 伺服器和編碼指南：</p>
<pre><code class="language-shell">php artisan boost:install
</code></pre>
<p><code>boost:install</code> 命令將為您在安裝過程中選擇的編碼代理生成相關的代理指南和技能檔案。</p>
<p>安裝 Laravel Boost 後，您就可以使用 Cursor、Claude Code 或您選擇的 AI 代理開始編碼了。</p>
<blockquote>
<p>[!NOTE]
您可以自由地將生成的 MCP 配置檔案（<code>.mcp.json</code>）、指南檔案（<code>CLAUDE.md</code>、<code>AGENTS.md</code>、<code>junie/</code> 等）和 <code>boost.json</code> 配置檔案添加到應用程式的 <code>.gitignore</code> 中，因為這些檔案在運行 <code>boost:install</code> 和 <code>boost:update</code> 時會自動重新生成。</p>
</blockquote>
<p><a name="set-up-your-agents"></a></p>
<h3>設定您的代理</h3>
<pre><code class="language-text">1. 打開命令面板（\`Cmd+Shift+P\` 或 \`Ctrl+Shift+P\`）
2. 在「/open MCP Settings」上按 \`enter\`
3. 開啟 \`laravel-boost\` 的開關
</code></pre>
<pre><code class="language-text">Claude Code 支援通常會自動啟用。如果您發現它沒有啟用，請在專案的目錄中打開一個終端並運行以下命令：

claude mcp add -s local -t stdio laravel-boost php artisan boost:mcp
</code></pre>
<pre><code class="language-text">Codex 支援通常會自動啟用。如果您發現它沒有啟用，請在專案的目錄中打開一個終端並運行以下命令：

codex mcp add laravel-boost -- php "artisan" "boost:mcp"
</code></pre>
<pre><code class="language-text">Gemini CLI 支援通常會自動啟用。如果您發現它沒有啟用，請在專案的目錄中打開一個終端並運行以下命令：

gemini mcp add -s project -t stdio laravel-boost php artisan boost:mcp
</code></pre>
<pre><code class="language-text">1. 打開命令面板（\`Cmd+Shift+P\` 或 \`Ctrl+Shift+P\`）
2. 在「MCP: List Servers」上按 \`enter\`
3. 箭頭移到 \`laravel-boost\` 並按 \`enter\`
4. 選擇「Start server」
</code></pre>
<pre><code class="language-text">1. 按兩次 \`shift\` 打開命令面板
2. 搜尋「MCP Settings」並按 \`enter\`
3. 勾選 \`laravel-boost\` 旁邊的方框
4. 點選右下角的「Apply」
</code></pre>
<p><a name="keeping-boost-resources-updated"></a></p>
<h3>保持 Boost 資源更新</h3>
<p>您可能希望定期更新本地的 Boost 資源（AI 指南和技能），以確保它們反映您已安裝的 Laravel 生態系統套件的最新版本。為此，您可以使用 <code>boost:update</code> Artisan 命令。</p>
<pre><code class="language-shell">php artisan boost:update
</code></pre>
<p>您也可以透過將其添加到 Composer 的「post-update-cmd」腳本中來自動化此過程：</p>
<pre><code class="language-json">{
  "scripts": {
    "post-update-cmd": [
      "@php artisan boost:update --ansi"
    ]
  }
}
</code></pre>
<p>預設情況下，<code>boost:update</code> 命令只會更新已在應用程式中發佈的現有 Boost 資源。如果您希望 Boost 掃描您的應用程式以查找任何新安裝的套件並提供發佈其相應的指南和技能，您可以使用 <code>--discover</code> 選項：</p>
<pre><code class="language-shell">php artisan boost:update --discover
</code></pre>
<p><a name="mcp-server"></a></p>
<h2>MCP 伺服器</h2>
<p>Laravel Boost 提供了一個 MCP（Model Context Protocol）伺服器，該伺服器為 AI 代理提供了與 Laravel 應用程式互動的工具。這些工具使代理能夠檢查應用程式的結構、查詢資料庫、執行程式碼等。</p>
<p><a name="available-mcp-tools"></a></p>
<h3>可用的 MCP 工具</h3>
<div class="overflow-auto"><table>
<thead>
<tr>
<th>名稱</th>
<th>說明</th>
</tr>
</thead>
<tbody><tr>
<td>Application Info</td>
<td>讀取 PHP 和 Laravel 版本、資料庫引擎、帶版本的生態系統套件列表以及 Eloquent 模型</td>
</tr>
<tr>
<td>Browser Logs</td>
<td>讀取瀏覽器中的日誌和錯誤</td>
</tr>
<tr>
<td>Database Connections</td>
<td>檢查可用的資料庫連線，包括預設連線</td>
</tr>
<tr>
<td>Database Query</td>
<td>對資料庫執行查詢</td>
</tr>
<tr>
<td>Database Schema</td>
<td>讀取資料庫結構</td>
</tr>
<tr>
<td>Get Absolute URL</td>
<td>將相對路徑的 URI 轉換為絕對路徑，以便代理生成有效的 URL</td>
</tr>
<tr>
<td>Last Error</td>
<td>從應用程式的日誌檔案中讀取最後一個錯誤</td>
</tr>
<tr>
<td>Read Log Entries</td>
<td>讀取最後 N 個日誌條目</td>
</tr>
<tr>
<td>Search Docs</td>
<td>查詢 Laravel 託管文件 API 服務，以根據已安裝的套件檢索文件</td>
</tr>
</tbody></table>
</div><p><a name="manually-registering-the-mcp-server"></a></p>
<h3>手動註冊 MCP 伺服器</h3>
<p>有時您可能需要手動向您選擇的編輯器註冊 Laravel Boost MCP 伺服器。您應該使用以下詳細資訊來註冊 MCP 伺服器：</p>
<table>
<tr><td><strong>Command</strong></td><td><code>php</code></td></tr>
<tr><td><strong>Args</strong></td><td><code>artisan boost:mcp</code></td></tr>
</table><p>JSON 範例：</p>
<pre><code class="language-json">{
    "mcpServers": {
        "laravel-boost": {
            "command": "php",
            "args": ["artisan", "boost:mcp"]
        }
    }
}
</code></pre>
<p><a name="ai-guidelines"></a></p>
<h2>AI 指南</h2>
<p>AI 指南是可組合的指令檔案，它們會預先載入，為 AI 代理提供關於 Laravel 生態系統套件的基本上下文。這些指南包含核心慣例、最佳實踐和框架特定的模式，幫助代理生成一致且高品質的程式碼。</p>
<p><a name="available-ai-guidelines"></a></p>
<h3>可用的 AI 指南</h3>
<p>Laravel Boost 包含以下套件和框架的 AI 指南。<code>core</code> 指南為 AI 提供了適用於所有版本的通用、概括性建議。</p>
<div class="overflow-auto"><table>
<thead>
<tr>
<th>套件</th>
<th>支援的版本</th>
</tr>
</thead>
<tbody><tr>
<td>Core &amp; Boost</td>
<td>core</td>
</tr>
<tr>
<td>Laravel Framework</td>
<td>core, 10.x, 11.x, 12.x, 13.x</td>
</tr>
<tr>
<td>Livewire</td>
<td>core, 2.x, 3.x, 4.x</td>
</tr>
<tr>
<td>Flux UI</td>
<td>core, free, pro</td>
</tr>
<tr>
<td>Folio</td>
<td>core</td>
</tr>
<tr>
<td>Herd</td>
<td>core</td>
</tr>
<tr>
<td>Inertia Laravel</td>
<td>core, 1.x, 2.x, 3.x</td>
</tr>
<tr>
<td>Inertia React</td>
<td>core, 1.x, 2.x, 3.x</td>
</tr>
<tr>
<td>Inertia Vue</td>
<td>core, 1.x, 2.x, 3.x</td>
</tr>
<tr>
<td>Inertia Svelte</td>
<td>core, 1.x, 2.x, 3.x</td>
</tr>
<tr>
<td>MCP</td>
<td>core</td>
</tr>
<tr>
<td>Pennant</td>
<td>core</td>
</tr>
<tr>
<td>Pest</td>
<td>core, 3.x, 4.x</td>
</tr>
<tr>
<td>PHPUnit</td>
<td>core</td>
</tr>
<tr>
<td>Pint</td>
<td>core</td>
</tr>
<tr>
<td>Sail</td>
<td>core</td>
</tr>
<tr>
<td>Tailwind CSS</td>
<td>core, 3.x, 4.x</td>
</tr>
<tr>
<td>Livewire Volt</td>
<td>core</td>
</tr>
<tr>
<td>Wayfinder</td>
<td>core</td>
</tr>
<tr>
<td>Enforce Tests</td>
<td>conditional</td>
</tr>
</tbody></table>
</div><blockquote>
<p><strong>注意：</strong> 有關保持 AI 指南更新的資訊，請參閱<a href="#keeping-boost-resources-upupdated">保持 Boost 資源更新</a>部分。</p>
</blockquote>
<p><a name="adding-custom-ai-guidelines"></a></p>
<h3>添加自訂 AI 指南</h3>
<p>要使用您自己的自訂 AI 指南來增強 Laravel Boost，請將 <code>.blade.php</code> 或 <code>.md</code> 檔案添加到應用程式的 <code>.ai/guidelines/*</code> 目錄中。當您在應用程式中運行 <code>boost:install</code> 時，這些檔案將自動包含在 Laravel Boost 的指南中。</p>
<p><a name="overriding-boost-ai-guidelines"></a></p>
<h3>覆蓋 Boost AI 指南</h3>
<p>您可以透過建立具有匹配檔案路徑的自訂指南來覆蓋 Boost 的內建 AI 指南。當您建立與現有 Boost 指南路徑匹配的自訂指南時，Boost 將使用您的自訂版本而不是內建版本。</p>
<p>例如，要覆蓋 Boost 的「Inertia React v2 Form Guidance」指南，請在 <code>.ai/guidelines/inertia-react/2/forms.blade.php</code> 建立一個檔案。當您在應用程式中運行 <code>boost:install</code> 時，Boost 將包含您的自訂指南而不是預設指南。</p>
<p><a name="third-party-package-ai-guidelines"></a></p>
<h3>第三方套件 AI 指南</h3>
<p>如果您維護一個第三方套件並希望 Boost 包含其 AI 指南，您可以透過在套件中添加 <code>resources/boost/guidelines/core.blade.php</code> 檔案來實現。當使用者運行 <code>php artisan boost:install</code> 時，Boost 將自動載入您的指南。</p>
<p>AI 指南應提供套件功能的簡短概述，概述任何必需的檔案結構或慣例，並說明如何建立或使用其主要功能（附帶範例命令或程式碼片段）。保持它們簡潔、可操作且專注於最佳實踐，以便 AI 能為使用者生成正確的程式碼。以下是一個範例：</p>
<pre><code class="language-php">## Package Name

This package provides [brief description of functionality].

### Features

- Feature 1: [clear &amp; short description].
- Feature 2: [clear &amp; short description]. Example usage:

@verbatim
&lt;code-snippet name="How to use Feature 2" lang="php"&gt;
$result = PackageName::featureTwo($param1, $param2);
&lt;/code-snippet&gt;
@endverbatim
</code></pre>
<p><a name="agent-skills"></a></p>
<h2>代理技能</h2>
<p><a href="https://agentskills.io/home">Agent Skills</a> 是輕量級的、針對性的知識模組，代理可以在處理特定領域時按需啟用。與預先載入的指南不同，技能允許僅在相關時才載入詳細的模式和最佳實踐，從而減少上下文膨脹並提高 AI 生成程式碼的相關性。</p>
<p>當您運行 <code>boost:install</code> 並選擇技能作為功能時，技能會根據 <code>composer.json</code> 中偵測到的套件自動安裝。例如，如果您的專案包含 <code>livewire/livewire</code>，<code>livewire-development</code> 技能將自動安裝。</p>
<p><a name="available-skills"></a></p>
<h3>可用技能</h3>
<div class="overflow-auto"><table>
<thead>
<tr>
<th>技能</th>
<th>套件</th>
</tr>
</thead>
<tbody><tr>
<td>fluxui-development</td>
<td>Flux UI</td>
</tr>
<tr>
<td>folio-routing</td>
<td>Folio</td>
</tr>
<tr>
<td>inertia-react-development</td>
<td>Inertia React</td>
</tr>
<tr>
<td>inertia-svelte-development</td>
<td>Inertia Svelte</td>
</tr>
<tr>
<td>inertia-vue-development</td>
<td>Inertia Vue</td>
</tr>
<tr>
<td>livewire-development</td>
<td>Livewire</td>
</tr>
<tr>
<td>mcp-development</td>
<td>MCP</td>
</tr>
<tr>
<td>pennant-development</td>
<td>Pennant</td>
</tr>
<tr>
<td>pest-testing</td>
<td>Pest</td>
</tr>
<tr>
<td>tailwindcss-development</td>
<td>Tailwind CSS</td>
</tr>
<tr>
<td>volt-development</td>
<td>Volt</td>
</tr>
<tr>
<td>wayfinder-development</td>
<td>Wayfinder</td>
</tr>
</tbody></table>
</div><blockquote>
<p><strong>注意：</strong> 有關保持技能更新的資訊，請參閱<a href="#keeping-boost-resources-updated">保持 Boost 資源更新</a>部分。</p>
</blockquote>
<p><a name="custom-skills"></a></p>
<h3>自訂技能</h3>
<p>要建立您自己的自訂技能，請將 <code>SKILL.md</code> 檔案添加到應用程式的 <code>.ai/skills/{skill-name}/</code> 目錄中。當您在應用程式中運行 <code>boost:update</code> 時，您的自訂技能將與 Boost 的內建技能一起安裝。</p>
<p>例如，要為應用程式的領域邏輯建立一個自訂技能：</p>
<pre><code>.ai/skills/creating-invoices/SKILL.md
</code></pre>
<p><a name="overriding-skills"></a></p>
<h3>覆蓋技能</h3>
<p>您可以透過建立具有匹配名稱的自訂技能來覆蓋 Boost 的內建技能。當您建立與現有 Boost 技能名稱匹配的自訂技能時，Boost 將使用您的自訂版本而不是內建版本。</p>
<p>例如，要覆蓋 Boost 的 <code>livewire-development</code> 技能，請在 <code>.ai/skills/livewire-development/SKILL.md</code> 建立一個檔案。當您在應用程式中運行 <code>boost:update</code> 時，Boost 將包含您的自訂技能而不是預設技能。</p>
<p><a name="third-party-package-skills"></a></p>
<h3>第三方套件技能</h3>
<p>如果您維護一個第三方套件並希望 Boost 包含其技能，您可以透過在套件中添加 <code>resources/boost/skills/{skill-name}/SKILL.md</code> 檔案來實現。當使用者運行 <code>php artisan boost:install</code> 時，Boost 將根據使用者的偏好自動安裝您的技能。</p>
<p>Boost 技能支援 <a href="https://agentskills.io/what-are-skills">Agent Skills 格式</a>，應結構化為包含一個 <code>SKILL.md</code> 檔案的資料夾，該檔案具有 YAML 前置元資料和 Markdown 指令。<code>SKILL.md</code> 檔案必須包含必需的前置元資料（<code>name</code> 和 <code>description</code>），並且可以選擇性地包含腳本、範本和參考資料。</p>
<p>技能應概述任何必需的檔案結構或慣例，並說明如何建立或使用其主要功能（附帶範例命令或程式碼片段）。保持它們簡潔、可操作且專注於最佳實踐，以便 AI 能為使用者生成正確的程式碼：</p>
<pre><code class="language-markdown">---
name: package-name-development
description: Build and work with PackageName features, including components and workflows.
---

# Package Name Development

## When to use this skill
Use this skill when working with PackageName features...

## Features

- Feature 1: [clear &amp; short description].
- Feature 2: [clear &amp; short description]. Example usage:

$result = PackageName::featureTwo($param1, $param2);
</code></pre>
<p><a name="guidelines-vs-skills"></a></p>
<h2>指南 vs. 技能</h2>
<p>Laravel Boost 提供了兩種不同的方式來為 AI 代理提供關於您應用程式的上下文：<strong>指南</strong>和<strong>技能</strong>。</p>
<p><strong>指南</strong>會在 AI 代理啟動時預先載入，提供有關 Laravel 慣例和最佳實踐的基本上下文，這些慣例和最佳實踐廣泛適用於您的程式碼庫。</p>
<p><strong>技能</strong>會在處理特定任務時按需啟用，包含特定領域（如 Livewire 元件或 Pest 測試）的詳細模式。僅在相關時載入技能可減少上下文膨脹並提高程式碼品質。</p>
<div class="overflow-auto"><table>
<thead>
<tr>
<th>方面</th>
<th>指南</th>
<th>技能</th>
</tr>
</thead>
<tbody><tr>
<td><strong>載入方式</strong></td>
<td>預先載入，始終存在</td>
<td>按需載入，僅在相關時</td>
</tr>
<tr>
<td><strong>範圍</strong></td>
<td>廣泛、基礎性</td>
<td>聚焦、任務特定</td>
</tr>
<tr>
<td><strong>目的</strong></td>
<td>核心慣例和最佳實踐</td>
<td>詳細的實作模式</td>
</tr>
</tbody></table>
</div><p><a name="documentation-api"></a></p>
<h2>文件 API</h2>
<p>Laravel Boost 包含一個文件 API，為 AI 代理提供了對包含超過 17,000 條 Laravel 特定資訊的廣泛知識庫的存取。該 API 使用帶有嵌入向量的語義搜尋來提供精確、上下文感知的結果。</p>
<p><code>Search Docs</code> MCP 工具允許代理查詢 Laravel 託管文件 API 服務，以根據您已安裝的套件檢索文件。Boost 的 AI 指南和技能將自動指示您的編碼代理使用此 API。</p>
<div class="overflow-auto"><table>
<thead>
<tr>
<th>套件</th>
<th>支援的版本</th>
</tr>
</thead>
<tbody><tr>
<td>Laravel Framework</td>
<td>10.x, 11.x, 12.x, 13.x</td>
</tr>
<tr>
<td>Filament</td>
<td>2.x, 3.x, 4.x, 5.x</td>
</tr>
<tr>
<td>Flux UI</td>
<td>2.x Free, 2.x Pro</td>
</tr>
<tr>
<td>Inertia</td>
<td>1.x, 2.x</td>
</tr>
<tr>
<td>Livewire</td>
<td>1.x, 2.x, 3.x, 4.x</td>
</tr>
<tr>
<td>Nova</td>
<td>4.x, 5.x</td>
</tr>
<tr>
<td>Pest</td>
<td>3.x, 4.x</td>
</tr>
<tr>
<td>Tailwind CSS</td>
<td>3.x, 4.x</td>
</tr>
</tbody></table>
</div><p><a name="extending-boost"></a></p>
<h2>擴展 Boost</h2>
<p>Boost 開箱即支援許多流行的 IDE 和 AI 代理。如果您的編碼工具尚未被支援，您可以建立自己的代理並將其與 Boost 整合。</p>
<p><a name="adding-support-for-other-ides-ai-agents"></a></p>
<h3>添加對其他 IDE / AI 代理的支援</h3>
<p>要添加對新 IDE 或 AI 代理的支援，請建立一個擴展 <code>Laravel\\Boost\\Install\\Agents\\Agent</code> 的類別，並根據您的需求實現以下一個或多個合約：</p>
<ul>
<li><code>Laravel\\Boost\\Contracts\\SupportsGuidelines</code> - 添加對 AI 指南的支援。</li>
<li><code>Laravel\\Boost\\Contracts\\SupportsMcp</code> - 添加對 MCP 的支援。</li>
<li><code>Laravel\\Boost\\Contracts\\SupportsSkills</code> - 添加對 Agent Skills 的支援。</li>
</ul>
<p><a name="writing-the-agent"></a></p>
<h4>編寫代理</h4>
<pre><code class="language-php">&lt;?php

declare(strict_types=1);

namespace App;

use Laravel\\Boost\\Contracts\\SupportsGuidelines;
use Laravel\\Boost\\Contracts\\SupportsMcp;
use Laravel\\Boost\\Contracts\\SupportsSkills;
use Laravel\\Boost\\Install\\Agents\\Agent;

class CustomAgent extends Agent implements SupportsGuidelines, SupportsMcp, SupportsSkills
{
    // 您的實作...
}
</code></pre>
<p>有關實作範例，請參閱 <a href="https://github.com/laravel/boost/blob/main/src/Install/Agents/ClaudeCode.php">ClaudeCode.php</a>。</p>
<p><a name="registering-the-agent"></a></p>
<h4>註冊代理</h4>
<p>在應用程式的 <code>App\\Providers\\AppServiceProvider</code> 的 <code>boot</code> 方法中註冊您的自訂代理：</p>
<pre><code class="language-php">use Laravel\\Boost\\Boost;

public function boot(): void
{
    Boost::registerAgent('customagent', CustomAgent::class);
}
</code></pre>
<p>註冊後，您的代理將在運行 <code>php artisan boost:install</code> 時可供選擇。</p>
`;
---

<DocsLayout title="Boost" currentPath="boost">
  <div class="mb-8">
    <nav class="text-sm text-text-secondary mb-4">
      <a href="/" class="hover:text-accent">首頁</a>
      <span class="mx-2">/</span>
      <span>文件</span>
      <span class="mx-2">/</span>
      <span>Boost</span>
    </nav>
    
    <h1 class="text-4xl font-bold mb-4">Boost</h1>
    
    <div class="bg-bg-secondary border border-border rounded-lg p-4 mb-6">
      <p class="text-text-secondary">
        📝 此頁面為 Laravel 官方文檔的繁體中文翻譯。
        <a href="https://laravel.com/docs/13.x/boost" class="text-accent" target="_blank" rel="noopener">
          查看原始英文版本
        </a>
      </p>
    </div>
  </div>

  <div class="prose prose-invert max-w-none" set:html={content}></div>
</DocsLayout>
