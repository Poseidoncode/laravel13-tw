---
import DocsLayout from '../../layouts/DocsLayout.astro';

const content = `<h1>Blade 範本</h1>
<ul>
<li><a href="#introduction">簡介</a><ul>
<li><a href="#supercharging-blade-with-livewire">使用 Livewire 增強 Blade</a></li>
</ul>
</li>
<li><a href="#displaying-data">顯示資料</a><ul>
<li><a href="#html-entity-encoding">HTML 實體編碼</a></li>
<li><a href="#blade-and-javascript-frameworks">Blade 和 JavaScript 框架</a></li>
</ul>
</li>
<li><a href="#blade-directives">Blade 指令</a><ul>
<li><a href="#if-statements">If 語句</a></li>
<li><a href="#switch-statements">Switch 語句</a></li>
<li><a href="#loops">迴圈</a></li>
<li><a href="#the-loop-variable">Loop 變數</a></li>
<li><a href="#conditional-classes">條件類別</a></li>
<li><a href="#additional-attributes">額外屬性</a></li>
<li><a href="#including-subviews">包含子視圖</a></li>
<li><a href="#the-once-directive"><code>@once</code> 指令</a></li>
<li><a href="#raw-php">原始 PHP</a></li>
<li><a href="#fonts">字體</a></li>
<li><a href="#comments">註解</a></li>
</ul>
</li>
<li><a href="#components">元件</a><ul>
<li><a href="#rendering-components">渲染元件</a></li>
<li><a href="#index-components">索引元件</a></li>
<li><a href="#passing-data-to-components">傳遞資料給元件</a></li>
<li><a href="#component-attributes">元件屬性</a></li>
<li><a href="#reserved-keywords">保留關鍵字</a></li>
<li><a href="#slots">插槽</a></li>
<li><a href="#inline-component-views">內聯元件視圖</a></li>
<li><a href="#dynamic-components">動態元件</a></li>
<li><a href="#manually-registering-components">手動註冊元件</a></li>
</ul>
</li>
<li><a href="#anonymous-components">匿名元件</a><ul>
<li><a href="#anonymous-index-components">匿名索引元件</a></li>
<li><a href="#data-properties-attributes">資料屬性 / 屬性</a></li>
<li><a href="#accessing-parent-data">存取父級資料</a></li>
<li><a href="#anonymous-component-paths">匿名元件路徑</a></li>
</ul>
</li>
<li><a href="#building-layouts">構建佈局</a><ul>
<li><a href="#layouts-using-components">使用元件構建佈局</a></li>
<li><a href="#layouts-using-template-inheritance">使用模板繼承構建佈局</a></li>
</ul>
</li>
<li><a href="#forms">表單</a><ul>
<li><a href="#csrf-field">CSRF 欄位</a></li>
<li><a href="#method-field">方法欄位</a></li>
<li><a href="#validation-errors">驗證錯誤</a></li>
</ul>
</li>
<li><a href="#stacks">堆疊</a></li>
<li><a href="#service-injection">服務注入</a></li>
<li><a href="#rendering-inline-blade-templates">渲染內聯 Blade 範本</a></li>
<li><a href="#rendering-blade-fragments">渲染 Blade 片段</a></li>
<li><a href="#extending-blade">擴展 Blade</a><ul>
<li><a href="#custom-echo-handlers">自訂 Echo 處理器</a></li>
<li><a href="#custom-if-statements">自訂 If 語句</a></li>
</ul>
</li>
</ul>
<p><a name="introduction"></a></p>
<h2>簡介</h2>
<p>Blade 是 Laravel 中包含的一個簡單但功能強大的範本引擎。與一些 PHP 範本引擎不同，Blade 不會限制您在範本中使用純 PHP 程式碼。事實上，所有 Blade 範本都會被編譯成純 PHP 程式碼並快取直到它們被修改，這意味著 Blade 幾乎為您的應用程式增加了零開銷。Blade 範本檔案使用 <code>.blade.php</code> 檔案副檔名，通常儲存在 <code>resources/views</code> 目錄中。</p>
<p>Blade 視圖可以透過全域 <code>view</code> 輔助函數從路由或控制器返回。當然，正如<a href="/docs/views">視圖</a>文檔中所述，資料可以透過 <code>view</code> 輔助函數的第二個參數傳遞給 Blade 視圖：</p>
<pre><code class="language-php">Route::get(&#39;/&#39;, function () {
    return view(&#39;greeting&#39;, [&#39;name&#39; =&gt; &#39;Finn&#39;]);
});
</code></pre>
<p><a name="supercharging-blade-with-livewire"></a></p>
<h3>使用 Livewire 增強 Blade</h3>
<p>想要將您的 Blade 範本提升到更高層級並輕鬆構建動態介面嗎？請查看 <a href="https://livewire.laravel.com">Laravel Livewire</a>。Livewire 允許您編寫增強了動態功能的 Blade 元件，這些功能通常只能透過像 React、Svelte 或 Vue 這樣的前端框架實現，提供了一種構建現代、響應式前端的好方法，而無需許多 JavaScript 框架的複雜性、客戶端渲染或構建步驟。</p>
<p><a name="displaying-data"></a></p>
<h2>顯示資料</h2>
<p>您可以透過將變數包裝在花括號中來顯示傳遞給 Blade 視圖的資料。例如，給定以下路由：</p>
<pre><code class="language-php">Route::get(&#39;/&#39;, function () {
    return view(&#39;welcome&#39;, [&#39;name&#39; =&gt; &#39;Samantha&#39;]);
});
</code></pre>
<p>您可以像這樣顯示 <code>name</code> 變數的內容：</p>
<pre><code class="language-blade">Hello, {{ $name }}.
</code></pre>
<blockquote>
<p>[!NOTE]
Blade 的 <code>{{ }}</code> echo 語句會自動透過 PHP 的 <code>htmlspecialchars</code> 函數來防止 XSS 攻擊。</p>
</blockquote>
<p>您不僅限於顯示傳遞給視圖的變數的內容。您還可以 echo 任何 PHP 函數的結果。事實上，您可以將任何您想要的 PHP 程式碼放在 Blade echo 語句中：</p>
<pre><code class="language-blade">The current UNIX timestamp is {{ time() }}.
</code></pre>
<p><a name="html-entity-encoding"></a></p>
<h3>HTML 實體編碼</h3>
<p>預設情況下，Blade（以及 Laravel 的 <code>e</code> 函數）會雙重編碼 HTML 實體。如果您想禁用雙重編碼，請從 <code>AppServiceProvider</code> 的 <code>boot</code> 方法中呼叫 <code>Blade::withoutDoubleEncoding</code> 方法：</p>
<pre><code class="language-php">&lt;?php

namespace App\\Providers;

use Illuminate\\Support\\Facades\\Blade;
use Illuminate\\Support\\ServiceProvider;

class AppServiceProvider extends ServiceProvider
{
    /**
     * 引導任何應用程式服務。
     */
    public function boot(): void
    {
        Blade::withoutDoubleEncoding();
    }
}
</code></pre>
<p><a name="displaying-unescaped-data"></a></p>
<h4>顯示未轉義的資料</h4>
<p>預設情況下，Blade <code>{{ }}</code> 語句會自動透過 PHP 的 <code>htmlspecialchars</code> 函數來防止 XSS 攻擊。如果您不希望您的資料被轉義，您可以使用以下語法：</p>
<pre><code class="language-blade">Hello, {!! $name !!}.
</code></pre>
<blockquote>
<p>[!WARNING]
在 echo 由應用程式用戶提供的內容時要非常小心。顯示用戶提供的資料時，您通常應使用轉義的、雙花括號語法來防止 XSS 攻擊。</p>
</blockquote>
<p><a name="blade-and-javascript-frameworks"></a></p>
<h3>Blade 和 JavaScript 框架</h3>
<p>由於許多 JavaScript 框架也使用「花括號」來指示應在瀏覽器中顯示給定表達式，您可以使用 <code>@</code> 符號來通知 Blade 渲染引擎表達式應保持不變。例如：</p>
<pre><code class="language-blade">&lt;h1&gt;Laravel&lt;/h1&gt;

Hello, @{{ name }}.
</code></pre>
<p>在此範例中，<code>@</code> 符號將被 Blade 移除；但是，<code>{{ name }}</code> 表達式將保持不變，允許由您的 JavaScript 框架渲染。</p>
<p><code>@</code> 符號也可用於轉義 Blade 指令：</p>
<pre><code class="language-blade">{{-- Blade template --}}
@@if()

&lt;!-- HTML output --&gt;
@if()
</code></pre>
<p><a name="rendering-json"></a></p>
<h4>渲染 JSON</h4>
<p>有時您可能希望將一個陣列傳遞給您的視圖，以便將其渲染為 JSON 來初始化一個 JavaScript 變數。例如：</p>
<pre><code class="language-php">&lt;script&gt;
    var app = &lt;?php echo json_encode($array); ?&gt;;
&lt;/script&gt;
</code></pre>
<p>但是，您可以使用 <code>Illuminate\\Support\\Js::from</code> 方法代替手動呼叫 <code>json_encode</code>。<code>from</code> 方法接受與 PHP 的 <code>json_encode</code> 函數相同的參數；但是，它將確保生成的 JSON 已被正確轉義以包含在 HTML 引號中。<code>from</code> 方法將返回一個字串 <code>JSON.parse</code> JavaScript 語句，該語句將把給定的物件或陣列轉換為有效的 JavaScript 物件：</p>
<pre><code class="language-blade">&lt;script&gt;
    var app = {{ Illuminate\\Support\\Js::from($array) }};
&lt;/script&gt;
</code></pre>
<p>Laravel 應用程式骨架的最新版本包含一個 <code>Js</code> Facade，它在您的 Blade 範本中提供了對此功能的便利存取：</p>
<pre><code class="language-blade">&lt;script&gt;
    var app = {{ Js::from($array) }};
&lt;/script&gt;
</code></pre>
<blockquote>
<p>[!WARNING]
您應該只使用 <code>Js::from</code> 方法將現有變數渲染為 JSON。Blade 範本基於正規表達式，嘗試將複雜表達式傳遞給指令可能會導致意外失敗。</p>
</blockquote>
<p><a name="the-at-verbatim-directive"></a></p>
<h4><code>@verbatim</code> 指令</h4>
<p>如果您在範本的大部分區域中顯示 JavaScript 變數，您可以將 HTML 包裝在 <code>@verbatim</code> 指令中，這樣您就不必為每個 Blade echo 語句添加 <code>@</code> 符號前綴：</p>
<pre><code class="language-blade">@verbatim
    &lt;div class=&quot;container&quot;&gt;
        Hello, {{ name }}.
    &lt;/div&gt;
@endverbatim
</code></pre>
<p><a name="blade-directives"></a></p>
<h2>Blade 指令</h2>
<p>除了模板繼承和顯示資料外，Blade 還為常見的 PHP 控制結構提供了便利的捷徑，例如條件語句和迴圈。這些捷徑提供了一種非常乾淨、簡潔的方式來處理 PHP 控制結構，同時也與其 PHP 對應物保持一致。</p>
<p><a name="if-statements"></a></p>
<h3>If 語句</h3>
<p>您可以使用 <code>@if</code>、<code>@elseif</code>、<code>@else</code> 和 <code>@endif</code> 指令來構建 <code>if</code> 語句。這些指令的功能與其 PHP 對應物完全相同：</p>
<pre><code class="language-blade">@if (count($records) === 1)
    I have one record!
@elseif (count($records) &gt; 1)
    I have multiple records!
@else
    I don&#39;t have any records!
@endif
</code></pre>
<p>為了方便，Blade 還提供了一個 <code>@unless</code> 指令：</p>
<pre><code class="language-blade">@unless (Auth::check())
    You are not signed in.
@endunless
</code></pre>
<p>除了已經討論過的條件指令外，<code>@isset</code> 和 <code>@empty</code> 指令可用於作為各自 PHP 函數的便利捷徑：</p>
<pre><code class="language-blade">@isset($records)
    // $records 已定義且不為 null...
@endisset

@empty($records)
    // $records 為「空」...
@endempty
</code></pre>
<p><a name="authentication-directives"></a></p>
<h4>身份驗證指令</h4>
<p><code>@auth</code> 和 <code>@guest</code> 指令可用於快速判斷當前用戶是否已<a href="/docs/authentication">通過身份驗證</a>或是否為訪客：</p>
<pre><code class="language-blade">@auth
    // 用戶已通過身份驗證...
@endauth

@guest
    // 用戶未通過身份驗證...
@endguest
</code></pre>
<p>如有需要，您可以在使用 <code>@auth</code> 和 <code>@guest</code> 指令時指定應檢查的身份驗證守衛：</p>
<pre><code class="language-blade">@auth(&#39;admin&#39;)
    // 用戶已通過身份驗證...
@endauth

@guest(&#39;admin&#39;)
    // 用戶未通過身份驗證...
@endguest
</code></pre>
<p><a name="environment-directives"></a></p>
<h4>環境指令</h4>
<p>您可以使用 <code>@production</code> 指令檢查應用程式是否在生產環境中運行：</p>
<pre><code class="language-blade">@production
    // 生產環境特定內容...
@endproduction
</code></pre>
<p>或者，您可以使用 <code>@env</code> 指令判斷應用程式是否在特定環境中運行：</p>
<pre><code class="language-blade">@env(&#39;staging&#39;)
    // 應用程式正在 &quot;staging&quot; 中運行...
@endenv

@env([&#39;staging&#39;, &#39;production&#39;])
    // 應用程式正在 &quot;staging&quot; 或 &quot;production&quot; 中運行...
@endenv
</code></pre>
<p><a name="section-directives"></a></p>
<h4>區段指令</h4>
<p>您可以使用 <code>@hasSection</code> 指令判斷模板繼承區段是否有內容：</p>
<pre><code class="language-blade">@hasSection(&#39;navigation&#39;)
    &lt;div class=&quot;pull-right&quot;&gt;
        @yield(&#39;navigation&#39;)
    &lt;/div&gt;

    &lt;div class=&quot;clearfix&quot;&gt;&lt;/div&gt;
@endif
</code></pre>
<p>您可以使用 <code>sectionMissing</code> 指令判斷區段是否沒有內容：</p>
<pre><code class="language-blade">@sectionMissing(&#39;navigation&#39;)
    &lt;div class=&quot;pull-right&quot;&gt;
        @include(&#39;default-navigation&#39;)
    &lt;/div&gt;
@endif
</code></pre>
<p><a name="session-directives"></a></p>
<h4>Session 指令</h4>
<p><code>@session</code> 指令可用於判斷<a href="/docs/session">session</a>值是否存在。如果 session 值存在，<code>@session</code> 和 <code>@endsession</code> 指令中的模板內容將被評估。在 <code>@session</code> 指令的內容中，您可以 echo <code>$value</code> 變數來顯示 session 值：</p>
<pre><code class="language-blade">@session(&#39;status&#39;)
    &lt;div class=&quot;p-4 bg-green-100&quot;&gt;
        {{ $value }}
    &lt;/div&gt;
@endsession
</code></pre>
<p><a name="context-directives"></a></p>
<h4>上下文指令</h4>
<p><code>@context</code> 指令可用於判斷<a href="/docs/context">上下文</a>值是否存在。如果上下文值存在，<code>@context</code> 和 <code>@endcontext</code> 指令中的模板內容將被評估。在 <code>@context</code> 指令的內容中，您可以 echo <code>$value</code> 變數來顯示上下文值：</p>
<pre><code class="language-blade">@context(&#39;canonical&#39;)
    &lt;link href=&quot;{{ $value }}&quot; rel=&quot;canonical&quot;&gt;
@endcontext
</code></pre>
<p><a name="switch-statements"></a></p>
<h3>Switch 語句</h3>
<p>可以使用 <code>@switch</code>、<code>@case</code>、<code>@break</code>、<code>@default</code> 和 <code>@endswitch</code> 指令來構建 Switch 語句：</p>
<pre><code class="language-blade">@switch($i)
    @case(1)
        First case...
        @break

    @case(2)
        Second case...
        @break

    @default
        Default case...
@endswitch
</code></pre>
<p><a name="loops"></a></p>
<h3>迴圈</h3>
<p>除了條件語句外，Blade 還為處理 PHP 的迴圈結構提供了簡單的指令。同樣，這些指令中的每一個都與其 PHP 對應物的功能完全相同：</p>
<pre><code class="language-blade">@for ($i = 0; $i &lt; 10; $i++)
    The current value is {{ $i }}
@endfor

@foreach ($users as $user)
    &lt;p&gt;This is user {{ $user-&gt;id }}&lt;/p&gt;
@endforeach

@forelse ($users as $user)
    &lt;li&gt;{{ $user-&gt;name }}&lt;/li&gt;
@empty
    &lt;p&gt;No users&lt;/p&gt;
@endforelse

@while (true)
    &lt;p&gt;I&#39;m looping forever.&lt;/p&gt;
@endwhile
</code></pre>
<blockquote>
<p>[!NOTE]
在迭代 <code>foreach</code> 迴圈時，您可以使用 <a href="#the-loop-variable">loop 變數</a>來獲取關於迴圈的有價值資訊，例如您是否處於迴圈的第一次或最後一次迭代中。</p>
</blockquote>
<p>使用迴圈時，您還可以使用 <code>@continue</code> 和 <code>@break</code> 指令來跳過當前迭代或結束迴圈：</p>
<pre><code class="language-blade">@foreach ($users as $user)
    @if ($user-&gt;type == 1)
        @continue
    @endif

    &lt;li&gt;{{ $user-&gt;name }}&lt;/li&gt;

    @if ($user-&gt;number == 5)
        @break
    @endif
@endforeach
</code></pre>
<p>您還可以在指令聲明中包含繼續或中斷條件：</p>
<pre><code class="language-blade">@foreach ($users as $user)
    @continue($user-&gt;type == 1)

    &lt;li&gt;{{ $user-&gt;name }}&lt;/li&gt;

    @break($user-&gt;number == 5)
@endforeach
</code></pre>
<p><a name="the-loop-variable"></a></p>
<h3>Loop 變數</h3>
<p>在迭代 <code>foreach</code> 迴圈時，迴圈內部將提供一個 <code>$loop</code> 變數。此變數提供了一些有用的資訊，例如當前迴圈索引以及這是否是迴圈的第一次或最後一次迭代：</p>
<pre><code class="language-blade">@foreach ($users as $user)
    @if ($loop-&gt;first)
        This is the first iteration.
    @endif

    @if ($loop-&gt;last)
        This is the last iteration.
    @endif

    &lt;p&gt;This is user {{ $user-&gt;id }}&lt;/p&gt;
@endforeach
</code></pre>
<p>如果您處於巢狀迴圈中，您可以透過 <code>parent</code> 屬性存取父迴圈的 <code>$loop</code> 變數：</p>
<pre><code class="language-blade">@foreach ($users as $user)
    @foreach ($user-&gt;posts as $post)
        @if ($loop-&gt;parent-&gt;first)
            This is the first iteration of the parent loop.
        @endif
    @endforeach
@endforeach
</code></pre>
<p><code>$loop</code> 變數還包含各種其他有用的屬性：</p>
<div class="overflow-auto"><table>
<thead>
<tr>
<th>屬性</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td><code>$loop-&gt;index</code></td>
<td>當前迴圈迭代的索引（從 0 開始）。</td>
</tr>
<tr>
<td><code>$loop-&gt;iteration</code></td>
<td>當前迴圈迭代（從 1 開始）。</td>
</tr>
<tr>
<td><code>$loop-&gt;remaining</code></td>
<td>迴圈中剩餘的迭代次數。</td>
</tr>
<tr>
<td><code>$loop-&gt;count</code></td>
<td>正在迭代的陣列中的項目總數。</td>
</tr>
<tr>
<td><code>$loop-&gt;first</code></td>
<td>這是否是迴圈的第一次迭代。</td>
</tr>
<tr>
<td><code>$loop-&gt;last</code></td>
<td>這是否是迴圈的最後一次迭代。</td>
</tr>
<tr>
<td><code>$loop-&gt;even</code></td>
<td>這是否是迴圈的偶數次迭代。</td>
</tr>
<tr>
<td><code>$loop-&gt;odd</code></td>
<td>這是否是迴圈的奇數次迭代。</td>
</tr>
<tr>
<td><code>$loop-&gt;depth</code></td>
<td>當前迴圈的巢狀級別。</td>
</tr>
<tr>
<td><code>$loop-&gt;parent</code></td>
<td>在巢狀迴圈中，父迴圈的迴圈變數。</td>
</tr>
</tbody></table>
</div><p><a name="conditional-classes"></a></p>
<h3>條件類別和樣式</h3>
<p><code>@class</code> 指令有條件地編譯一個 CSS 類別字串。該指令接受一個類別數組，其中數組鍵包含您希望添加的類別或類別，而值是一個布林表達式。如果數組元素具有數字鍵，它將始終包含在渲染的類別列表中：</p>
<pre><code class="language-blade">@php
    $isActive = false;
    $hasError = true;
@endphp

&lt;span @class([
    &#39;p-4&#39;,
    &#39;font-bold&#39; =&gt; $isActive,
    &#39;text-gray-500&#39; =&gt; ! $isActive,
    &#39;bg-red&#39; =&gt; $hasError,
])&gt;&lt;/span&gt;

&lt;span class=&quot;p-4 text-gray-500 bg-red&quot;&gt;&lt;/span&gt;
</code></pre>
<p>同樣地，<code>@style</code> 指令可用於有條件地向 HTML 元素添加內聯 CSS 樣式：</p>
<pre><code class="language-blade">@php
    $isActive = true;
@endphp

&lt;span @style([
    &#39;background-color: red&#39;,
    &#39;font-weight: bold&#39; =&gt; $isActive,
])&gt;&lt;/span&gt;

&lt;span style=&quot;background-color: red; font-weight: bold;&quot;&gt;&lt;/span&gt;
</code></pre>
<p><a name="additional-attributes"></a></p>
<h3>額外屬性</h3>
<p>為了方便，您可以使用 <code>@checked</code> 指令輕鬆指示給定的 HTML 核取方塊輸入是否為「已選取」。如果提供的條件評估為 <code>true</code>，此指令將 echo <code>checked</code>：</p>
<pre><code class="language-blade">&lt;input
    type=&quot;checkbox&quot;
    name=&quot;active&quot;
    value=&quot;active&quot;
    @checked(old(&#39;active&#39;, $user-&gt;active))
/&gt;
</code></pre>
<p>同樣地，<code>@selected</code> 指令可用於指示給定的選擇選項是否應為「已選取」：</p>
<pre><code class="language-blade">&lt;select name=&quot;version&quot;&gt;
    @foreach ($product-&gt;versions as $version)
        &lt;option value=&quot;{{ $version }}&quot; @selected(old(&#39;version&#39;) == $version)&gt;
            {{ $version }}
        &lt;/option&gt;
    @endforeach
&lt;/select&gt;
</code></pre>
<p>此外，<code>@disabled</code> 指令可用於指示給定的元素是否應為「已禁用」：</p>
<pre><code class="language-blade">&lt;button type=&quot;submit&quot; @disabled($errors-&gt;isNotEmpty())&gt;Submit&lt;/button&gt;
</code></pre>
<p>此外，<code>@readonly</code> 指令可用於指示給定的元素是否應為「唯讀」：</p>
<pre><code class="language-blade">&lt;input
    type=&quot;email&quot;
    name=&quot;email&quot;
    value=&quot;email@laravel.com&quot;
    @readonly($user-&gt;isNotAdmin())
/&gt;
</code></pre>
<p>此外，<code>@required</code> 指令可用於指示給定的元素是否應為「必填」：</p>
<pre><code class="language-blade">&lt;input
    type=&quot;text&quot;
    name=&quot;title&quot;
    value=&quot;title&quot;
    @required($user-&gt;isAdmin())
/&gt;
</code></pre>
<p><a name="including-subviews"></a></p>
<h3>包含子視圖</h3>
<blockquote>
<p>[!NOTE]
雖然您可以自由使用 <code>@include</code> 指令，但 Blade <a href="#components">元件</a>提供了類似的功能，並提供了幾個優於 <code>@include</code> 指令的好處，例如資料和屬性綁定。</p>
</blockquote>
<p>Blade 的 <code>@include</code> 指令允許您從另一個視圖中包含一個 Blade 視圖。父視圖中可用的所有變數都將在包含的視圖中可用：</p>
<pre><code class="language-blade">&lt;div&gt;
    @include(&#39;shared.errors&#39;)

    &lt;form&gt;
        &lt;!-- Form Contents --&gt;
    &lt;/form&gt;
&lt;/div&gt;
</code></pre>
<p>即使包含的視圖將繼承父視圖中可用的所有資料，您還可以傳遞一個應在包含的視圖中可用的額外資料數組：</p>
<pre><code class="language-blade">@include(&#39;view.name&#39;, [&#39;status&#39; =&gt; &#39;complete&#39;])
</code></pre>
<p>如果您嘗試 <code>@include</code> 一個不存在的視圖，Laravel 將拋出一個錯誤。如果您想包含一個可能存在也可能不存在的視圖，您應該使用 <code>@includeIf</code> 指令：</p>
<pre><code class="language-blade">@includeIf(&#39;view.name&#39;, [&#39;status&#39; =&gt; &#39;complete&#39;])
</code></pre>
<p>如果您想在給定的布林表達式評估為 <code>true</code> 或 <code>false</code> 時 <code>@include</code> 一個視圖，您可以使用 <code>@includeWhen</code> 和 <code>@includeUnless</code> 指令：</p>
<pre><code class="language-blade">@includeWhen($boolean, &#39;view.name&#39;, [&#39;status&#39; =&gt; &#39;complete&#39;])

@includeUnless($boolean, &#39;view.name&#39;, [&#39;status&#39; =&gt; &#39;complete&#39;])
</code></pre>
<p>要從給定的視圖數組中包含第一個存在的視圖，您可以使用 <code>includeFirst</code> 指令：</p>
<pre><code class="language-blade">@includeFirst([&#39;custom.admin&#39;, &#39;admin&#39;], [&#39;status&#39; =&gt; &#39;complete&#39;])
</code></pre>
<p>如果您想包含一個視圖而不從父視圖繼承任何變數，您可以使用 <code>@includeIsolated</code> 指令。包含的視圖將只能存取您明確傳遞的變數：</p>
<pre><code class="language-blade">@includeIsolated(&#39;view.name&#39;, [&#39;user&#39; =&gt; $user])
</code></pre>
<blockquote>
<p>[!WARNING]
您應該避免在 Blade 視圖中使用 <code>__DIR__</code> 和 <code>__FILE__</code> 常數，因為它們將引用快取的、編譯的視圖的位置。</p>
</blockquote>
<p><a name="rendering-views-for-collections"></a></p>
<h4>為集合渲染視圖</h4>
<p>您可以使用 Blade 的 <code>@each</code> 指令將迴圈和包含合併到一行中：</p>
<pre><code class="language-blade">@each(&#39;view.name&#39;, $jobs, &#39;job&#39;)
</code></pre>
<p><code>@each</code> 指令的第一個參數是要為陣列或集合中的每個元素渲染的視圖。第二個參數是您要迭代的陣列或集合，而第三個參數是將在視圖中分配給當前迭代的變數名稱。因此，例如，如果您正在迭代 <code>jobs</code> 陣列，通常您會希望在視圖中將每個 job 作為 <code>job</code> 變數存取。當前迭代的陣列金鑰將作為 <code>key</code> 變數在視圖中可用。</p>
<p>您還可以向 <code>@each</code> 指令傳遞第四個參數。此參數確定如果給定陣列為空時將渲染的視圖。</p>
<pre><code class="language-blade">@each(&#39;view.name&#39;, $jobs, &#39;job&#39;, &#39;view.empty&#39;)
</code></pre>
<blockquote>
<p>[!WARNING]
透過 <code>@each</code> 渲染的視圖不會繼承父視圖中的變數。如果子視圖需要這些變數，您應該改用 <code>@foreach</code> 和 <code>@include</code> 指令。</p>
</blockquote>
<p><a name="the-once-directive"></a></p>
<h3><code>@once</code> 指令</h3>
<p><code>@once</code> 指令允許您定義模板的一部分，該部分每個渲染週期只會被評估一次。這對於使用<a href="#stacks">堆疊</a>將給定的 JavaScript 推送到頁面的標頭中非常有用。例如，如果您在迴圈中渲染給定的<a href="#components">元件</a>，您可能希望只在元件第一次渲染時將 JavaScript 推送到標頭：</p>
<pre><code class="language-blade">@once
    @push(&#39;scripts&#39;)
        &lt;script&gt;
            // Your custom JavaScript...
        &lt;/script&gt;
    @endpush
@endonce
</code></pre>
<p>由於 <code>@once</code> 指令通常與 <code>@push</code> 或 <code>@prepend</code> 指令結合使用，因此 <code>@pushOnce</code> 和 <code>@prependOnce</code> 指令可為您提供便利：</p>
<pre><code class="language-blade">@pushOnce(&#39;scripts&#39;)
    &lt;script&gt;
        // Your custom JavaScript...
    &lt;/script&gt;
@endPushOnce
</code></pre>
<p>如果您正在從兩個單獨的 Blade 範本中推送重複的內容，您應該提供一個唯一標識符作為 <code>@pushOnce</code> 指令的第二個參數，以確保內容只渲染一次：</p>
<pre><code class="language-blade">&lt;!-- pie-chart.blade.php --&gt;
@pushOnce(&#39;scripts&#39;, &#39;chart.js&#39;)
    &lt;script src=&quot;/chart.js&quot;&gt;&lt;/script&gt;
@endPushOnce

&lt;!-- line-chart.blade.php --&gt;
@pushOnce(&#39;scripts&#39;, &#39;chart.js&#39;)
    &lt;script src=&quot;/chart.js&quot;&gt;&lt;/script&gt;
@endPushOnce
</code></pre>
<p><a name="raw-php"></a></p>
<h3>原始 PHP</h3>
<p>在某些情況下，將 PHP 程式碼嵌入到您的視圖中很有用。您可以使用 Blade 的 <code>@php</code> 指令在模板中運行一段純 PHP：</p>
<pre><code class="language-blade">@php
    $counter = 1;
@endphp
</code></pre>
<p>或者，如果您只需要使用 PHP 來匯入一個類別，您可以使用 <code>@use</code> 指令：</p>
<pre><code class="language-blade">@use(&#39;App\\Models\\Flight&#39;)
</code></pre>
<p>可以向 <code>@use</code> 指令提供第二個參數來為匯入的類別設置別名：</p>
<pre><code class="language-blade">@use(&#39;App\\Models\\Flight&#39;, &#39;FlightModel&#39;)
</code></pre>
<p>如果您在相同命名空間中有多個類別，您可以將這些類別的匯入分組：</p>
<pre><code class="language-blade">@use(&#39;App\\Models\\{Flight, Airport}&#39;)
</code></pre>
<p><code>@use</code> 指令還支援透過在匯入路徑前添加 <code>function</code> 或 <code>const</code> 修飾符來匯入 PHP 函數和常數：</p>
<pre><code class="language-blade">@use(function App\\Helpers\\format_currency)
@use(const App\\Constants\\MAX_ATTEMPTS)
</code></pre>
<p>與類別匯入一樣，函數和常數也支援別名：</p>
<pre><code class="language-blade">@use(function App\\Helpers\\format_currency, &#39;formatMoney&#39;)
@use(const App\\Constants\\MAX_ATTEMPTS, &#39;MAX_TRIES&#39;)
</code></pre>
<p>函數和 const 修飾符也支援分組匯入，允許您在單個指令中從相同命名空間匯入多個符號：</p>
<pre><code class="language-blade">@use(function App\\Helpers\\{format_currency, format_date})
@use(const App\\Constants\\{MAX_ATTEMPTS, DEFAULT_TIMEOUT})
</code></pre>
<p><a name="fonts"></a></p>
<h3>字體</h3>
<p>使用<a href="/docs/vite#working-with-fonts"> Laravel 的 Vite 字體優化</a>時，您可以使用 <code>@fonts</code> 指令在應用程式的佈局中渲染您配置的字體預載入連結和內聯字體 CSS：</p>
<pre><code class="language-blade">&lt;!doctype html&gt;
&lt;head&gt;
    {{-- ... --}}

    @fonts
    @vite(&#39;resources/js/app.js&#39;)
&lt;/head&gt;
</code></pre>
<p><code>@fonts</code> 指令渲染在 <code>vite.config.js</code> 檔案中配置的所有字體系列。該指令通常應放在應用程式根佈局的 <code>&lt;head&gt;</code> 中，在使用這些字體的任何內容之前。</p>
<p>如果頁面只需要您配置的字體中的一部分，您可以向指令傳遞一個或多個字體別名：</p>
<pre><code class="language-blade">{{-- 載入單個字體別名... --}}
@fonts(&#39;sans&#39;)

{{-- 載入多個字體別名... --}}
@fonts([&#39;sans&#39;, &#39;mono&#39;])
</code></pre>
<p>字體別名是在 Vite 配置中定義字體時使用 <code>alias</code> 選項配置的。<code>@fonts</code> 指令呼叫 <code>Vite</code> Facade 提供的 <code>fonts</code> 方法，該方法也可以直接調用：</p>
<pre><code class="language-blade">{{ Vite::fonts([&#39;sans&#39;, &#39;mono&#39;]) }}
</code></pre>
<p><a name="comments"></a></p>
<h3>註解</h3>
<p>Blade 還允許您在視圖中定義註解。但是，與 HTML 註解不同，Blade 註解不包含在應用程式返回的 HTML 中：</p>
<pre><code class="language-blade">{{-- This comment will not be present in the rendered HTML --}}
</code></pre>
<p><a name="components"></a></p>
<h2>元件</h2>
<p>元件和插槽提供了與區段、佈局和包含類似的好處；但是，有些人可能發現元件和插槽的心智模型更容易理解。編寫元件有兩種方法：基於類別的元件和匿名元件。</p>
<p>要創建基於類別的元件，您可以使用 <code>make:component</code> Artisan 命令。為了說明如何使用元件，我們將創建一個簡單的 <code>Alert</code> 元件。<code>make:component</code> 命令將把元件放在 <code>app/View/Components</code> 目錄中：</p>
<pre><code class="language-shell">php artisan make:component Alert
</code></pre>
<p><code>make:component</code> 命令還將為元件創建一個視圖模板。視圖將放在 <code>resources/views/components</code> 目錄中。為您自己的應用程式編寫元件時，元件會在 <code>app/View/Components</code> 目錄和 <code>resources/views/components</code> 目錄中被自動發現，因此通常不需要進一步的元件註冊。</p>
<p>您還可以在子目錄中創建元件：</p>
<pre><code class="language-shell">php artisan make:component Forms/Input
</code></pre>
<p>上面的命令將在 <code>app/View/Components/Forms</code> 目錄中創建一個 <code>Input</code> 元件，視圖將放在 <code>resources/views/components/forms</code> 目錄中。</p>
<p><a name="manually-registering-package-components"></a></p>
<h4>手動註冊套件元件</h4>
<p>為您自己的應用程式編寫元件時，元件會在 <code>app/View/Components</code> 目錄和 <code>resources/views/components</code> 目錄中被自動發現。</p>
<p>但是，如果您正在構建一個使用 Blade 元件的套件，您需要手動註冊您的元件類別及其 HTML 標籤別名。您應該通常在套件的服務提供者的 <code>boot</code> 方法中註冊您的元件：</p>
<pre><code class="language-php">use Illuminate\\Support\\Facades\\Blade;

/**
 * 引導套件的服務。
 */
public function boot(): void
{
    Blade::component(&#39;package-alert&#39;, Alert::class);
}
</code></pre>
<p>一旦您的元件被註冊，就可以使用其標籤別名來渲染它：</p>
<pre><code class="language-blade">&lt;x-package-alert/&gt;
</code></pre>
<p>或者，您可以使用 <code>componentNamespace</code> 方法按慣例自動載入元件類別。例如，<code>Nightshade</code> 套件可能有一個位於 <code>Package\\Views\\Components</code> 命名空間中的 <code>Calendar</code> 和 <code>ColorPicker</code> 元件：</p>
<pre><code class="language-php">use Illuminate\\Support\\Facades\\Blade;

/**
 * 引導套件的服務。
 */
public function boot(): void
{
    Blade::componentNamespace(&#39;Nightshade\\\\Views\\\\Components&#39;, &#39;nightshade&#39;);
}
</code></pre>
<p>這將允許使用其供應商命名空間使用 <code>package-name::</code> 語法來使用套件元件：</p>
<pre><code class="language-blade">&lt;x-nightshade::calendar /&gt;
&lt;x-nightshade::color-picker /&gt;
</code></pre>
<p>Blade 將透過將元件名稱轉換為 PascalCase 來自動偵測與該元件關聯的類別。也支援使用「點」語法的子目錄。</p>
<p><a name="anonymous-components"></a></p>
<h2>匿名元件</h2>
<p>與內聯元件類似，匿名元件提供了一種透過單個檔案管理元件的機制。但是，匿名元件使用一個視圖檔案且沒有關聯的類別。要定義匿名元件，您只需要將 Blade 模板放在 <code>resources/views/components</code> 目錄中。例如，假設您在 <code>resources/views/components/alert.blade.php</code> 定義了一個元件，您可以像這樣渲染它：</p>
<pre><code class="language-blade">&lt;x-alert/&gt;
</code></pre>
<p>您可以使用 <code>.</code> 字元來指示元件是否巢狀在 <code>components</code> 目錄的更深處。例如，假設元件定義在 <code>resources/views/components/inputs/button.blade.php</code>，您可以像這樣渲染它：</p>
<pre><code class="language-blade">&lt;x-inputs.button/&gt;
</code></pre>
<p>要透過 Artisan 創建匿名元件，您可以在呼叫 <code>make:component</code> 命令時使用 <code>--view</code> 標誌：</p>
<pre><code class="language-shell">php artisan make:component forms.input --view
</code></pre>
<p>上面的命令將在 <code>resources/views/components/forms/input.blade.php</code> 創建一個 Blade 檔案，可以透過 <code>&lt;x-forms.input /&gt;</code> 作為元件渲染。</p>
<p><a name="anonymous-index-components"></a></p>
<h3>匿名索引元件</h3>
<p>有時，當一個元件由多個 Blade 模板組成時，您可能希望將給定元件的模板分組在單個目錄中。例如，想像一個具有以下目錄結構的「手風琴」元件：</p>
<pre><code class="language-text">/resources/views/components/accordion.blade.php
/resources/views/components/accordion/item.blade.php
</code></pre>
<p>此目錄結構允許您像這樣渲染手風琴元件及其項目：</p>
<pre><code class="language-blade">&lt;x-accordion&gt;
    &lt;x-accordion.item&gt;
        ...
    &lt;/x-accordion.item&gt;
&lt;/x-accordion&gt;
</code></pre>
<p>但是，為了透過 <code>x-accordion</code> 渲染手風琴元件，我們被迫將「索引」手風琴元件模板放在 <code>resources/views/components</code> 目錄中，而不是與其他手風琴相關模板一起巢狀在 <code>accordion</code> 目錄中。</p>
<p>幸運的是，Blade 允許您在元件的目錄本身內放置一個與元件目錄名稱匹配的檔案。當此模板存在時，即使它巢狀在目錄中，也可以作為元件的「根」元素渲染。因此，我們可以繼續使用上面範例中給出的相同 Blade 語法；但是，我們將調整我們的目錄結構如下：</p>
<pre><code class="language-text">/resources/views/components/accordion/accordion.blade.php
/resources/views/components/accordion/item.blade.php
</code></pre>
<p><a name="data-properties-attributes"></a></p>
<h3>資料屬性 / 屬性</h3>
<p>由於匿名元件沒有任何關聯的類別，您可能想知道如何區分哪些資料應作為變數傳遞給元件，以及哪些屬性應放在元件的<a href="#component-attributes">屬性包</a>中。</p>
<p>您可以使用 <code>@props</code> 指令在元件的 Blade 模板頂部指定哪些屬性應被視為資料變數。元件上的所有其他屬性將透過元件的屬性包可用。如果您想給資料變數一個預設值，您可以將變數名稱指定為數組鍵，將預設值指定為數組值：</p>
<pre><code class="language-blade">&lt;!-- /resources/views/components/alert.blade.php --&gt;

@props([&#39;type&#39; =&gt; &#39;info&#39;, &#39;message&#39;])

&lt;div {{ $attributes-&gt;merge([&#39;class&#39; =&gt; &#39;alert alert-&#39;.$type]) }}&gt;
    {{ $message }}
&lt;/div&gt;
</code></pre>
<p>給定上面的元件定義，我們可以像這樣渲染元件：</p>
<pre><code class="language-blade">&lt;x-alert type=&quot;error&quot; :message=&quot;$message&quot; class=&quot;mb-4&quot;/&gt;
</code></pre>
<p><a name="accessing-parent-data"></a></p>
<h3>存取父級資料</h3>
<p>有時您可能希望在子元件內部存取來自父元件的資料。在這些情況下，您可以使用 <code>@aware</code> 指令。例如，想像我們正在構建一個由父級 <code>&lt;x-menu&gt;</code> 和子級 <code>&lt;x-menu.item&gt;</code> 組成的複雜選單元件：</p>
<pre><code class="language-blade">&lt;x-menu color=&quot;purple&quot;&gt;
    &lt;x-menu.item&gt;...&lt;/x-menu.item&gt;
    &lt;x-menu.item&gt;...&lt;/x-menu.item&gt;
&lt;/x-menu&gt;
</code></pre>
<p><code>&lt;x-menu&gt;</code> 元件可能具有如下所示的實現：</p>
<pre><code class="language-blade">&lt;!-- /resources/views/components/menu/index.blade.php --&gt;

@props([&#39;color&#39; =&gt; &#39;gray&#39;])

&lt;ul {{ $attributes-&gt;merge([&#39;class&#39; =&gt; &#39;bg-&#39;.$color.&#39;-200&#39;]) }}&gt;
    {{ $slot }}
&lt;/ul&gt;
</code></pre>
<p>由於 <code>color</code> 屬性只傳遞給了父級（<code>&lt;x-menu&gt;</code>），因此它在 <code>&lt;x-menu.item&gt;</code> 中不可用。但是，如果我們使用 <code>@aware</code> 指令，我們也可以使其在 <code>&lt;x-menu.item&gt;</code> 中可用：</p>
<pre><code class="language-blade">&lt;!-- /resources/views/components/menu/item.blade.php --&gt;

@aware([&#39;color&#39; =&gt; &#39;gray&#39;])

&lt;li {{ $attributes-&gt;merge([&#39;class&#39; =&gt; &#39;text-&#39;.$color.&#39;-800&#39;]) }}&gt;
    {{ $slot }}
&lt;/li&gt;
</code></pre>
<blockquote>
<p>[!WARNING]
<code>@aware</code> 指令無法存取未透過 HTML 屬性明確傳遞給父元件的父級資料。未明確傳遞給父元件的預設 <code>@props</code> 值無法被 <code>@aware</code> 指令存取。</p>
</blockquote>
<p><a name="anonymous-component-paths"></a></p>
<h3>匿名元件路徑</h3>
<p>如前所述，匿名元件通常透過將 Blade 模板放在 <code>resources/views/components</code> 目錄中來定義。但是，您可能偶爾希望向 Laravel 註冊其他匿名元件路徑，除了預設路徑之外。</p>
<p><code>anonymousComponentPath</code> 方法接受匿名元件位置的「路徑」作為其第一個參數，以及一個可選的「命名空間」作為其第二個參數，元件應被放置在該命名空間下。通常，此方法應從應用程式的其中一個<a href="/docs/providers">服務提供者</a>的 <code>boot</code> 方法中呼叫：</p>
<pre><code class="language-php">/**
 * 引導任何應用程式服務。
 */
public function boot(): void
{
    Blade::anonymousComponentPath(__DIR__.&#39;/../components&#39;);
}
</code></pre>
<p>當元件路徑在沒有指定前綴的情況下註冊時，它們也可以在沒有相應前綴的情況下在您的 Blade 元件中渲染。例如，如果在上面註冊的路徑中存在一個 <code>panel.blade.php</code> 元件，它可以像這樣渲染：</p>
<pre><code class="language-blade">&lt;x-panel /&gt;
</code></pre>
<p>前綴「命名空間」可以作為 <code>anonymousComponentPath</code> 方法的第二個參數提供：</p>
<pre><code class="language-php">Blade::anonymousComponentPath(__DIR__.&#39;/../components&#39;, &#39;dashboard&#39;);
</code></pre>
<p>當提供前綴時，該「命名空間」內的元件可以透過在渲染元件時將元件的命名空間添加到元件名稱之前來渲染：</p>
<pre><code class="language-blade">&lt;x-dashboard::panel /&gt;
</code></pre>
<p><a name="building-layouts"></a></p>
<h2>構建佈局</h2>
<p><a name="layouts-using-components"></a></p>
<h3>使用元件構建佈局</h3>
<p>大多數 Web 應用程式在各種頁面上維護相同的通用佈局。如果我們必須在我們創建的每個視圖中重複整個佈局 HTML，那將是極其繁瑣且難以維護的。幸運的是，將此佈局定義為單個<a href="#components">Blade 元件</a>然後在整個應用程式中使用它是很方便的。</p>
<p><a name="defining-the-layout-component"></a></p>
<h4>定義佈局元件</h4>
<p>例如，想像我們正在構建一個「待辦事項」清單應用程式。我們可能會定義一個如下所示的 <code>layout</code> 元件：</p>
<pre><code class="language-blade">&lt;!-- resources/views/components/layout.blade.php --&gt;

&lt;html&gt;
    &lt;head&gt;
        &lt;title&gt;{{ $title ?? &#39;Todo Manager&#39; }}&lt;/title&gt;
    &lt;/head&gt;
    &lt;body&gt;
        &lt;h1&gt;Todos&lt;/h1&gt;
        &lt;hr/&gt;
        {{ $slot }}
    &lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p><a name="applying-the-layout-component"></a></p>
<h4>應用佈局元件</h4>
<p>定義 <code>layout</code> 元件後，我們可以創建一個使用該元件的 Blade 視圖。在此範例中，我們將定義一個顯示任務清單的簡單視圖：</p>
<pre><code class="language-blade">&lt;!-- resources/views/tasks.blade.php --&gt;

&lt;x-layout&gt;
    @foreach ($tasks as $task)
        &lt;div&gt;{{ $task }}&lt;/div&gt;
    @endforeach
&lt;/x-layout&gt;
</code></pre>
<p>請記住，注入到元件中的內容將提供給 <code>layout</code> 元件中的預設 <code>$slot</code> 變數。正如您可能已經注意到的，我們的 <code>layout</code> 還尊重 <code>$title</code> 插槽（如果提供了的話）；否則，將顯示預設標題。我們可以使用<a href="#components">元件文檔</a>中討論的標準插槽語法從任務清單視圖注入自訂標題：</p>
<pre><code class="language-blade">&lt;!-- resources/views/tasks.blade.php --&gt;

&lt;x-layout&gt;
    &lt;x-slot:title&gt;
        Custom Title
    &lt;/x-slot&gt;

    @foreach ($tasks as $task)
        &lt;div&gt;{{ $task }}&lt;/div&gt;
    @endforeach
&lt;/x-layout&gt;
</code></pre>
<p>現在我們已經定義了佈局和任務清單視圖，我們只需要從路由返回 <code>task</code> 視圖：</p>
<pre><code class="language-php">use App\\Models\\Task;

Route::get(&#39;/tasks&#39;, function () {
    return view(&#39;tasks&#39;, [&#39;tasks&#39; =&gt; Task::all()]);
});
</code></pre>
<p><a name="layouts-using-template-inheritance"></a></p>
<h3>使用模板繼承構建佈局</h3>
<p><a name="defining-a-layout"></a></p>
<h4>定義佈局</h4>
<p>佈局也可以透過「模板繼承」來創建。這是<a href="#components">元件</a>引入之前構建應用程式的主要方式。</p>
<p>首先，讓我們看一個簡單的範例。首先，我們將檢查一個頁面佈局。由於大多數 Web 應用程式在各種頁面上維護相同的通用佈局，將此佈局定義為單個 Blade 視圖是很方便的：</p>
<pre><code class="language-blade">&lt;!-- resources/views/layouts/app.blade.php --&gt;

&lt;html&gt;
    &lt;head&gt;
        &lt;title&gt;App Name - @yield(&#39;title&#39;)&lt;/title&gt;
    &lt;/head&gt;
    &lt;body&gt;
        @section(&#39;sidebar&#39;)
            This is the master sidebar.
        @show

        &lt;div class=&quot;container&quot;&gt;
            @yield(&#39;content&#39;)
        &lt;/div&gt;
    &lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p>如您所見，此檔案包含典型的 HTML 標記。但是，請注意 <code>@section</code> 和 <code>@yield</code> 指令。<code>@section</code> 指令（顧名思義）定義了一個內容區段，而 <code>@yield</code> 指令用於顯示給定區段的內容。</p>
<p>現在我們已經為應用程式定義了一個佈局，讓我們定義一個繼承該佈局的子頁面。</p>
<p><a name="extending-a-layout"></a></p>
<h4>擴展佈局</h4>
<p>定義子視圖時，使用 <code>@extends</code> Blade 指令指定子視圖應「繼承」哪個佈局。擴展 Blade 佈局的視圖可以使用 <code>@section</code> 指令將內容注入到佈局的區段中。請記住，如上面的範例所示，這些區段的內容將使用 <code>@yield</code> 在佈局中顯示：</p>
<pre><code class="language-blade">&lt;!-- resources/views/child.blade.php --&gt;

@extends(&#39;layouts.app&#39;)

@section(&#39;title&#39;, &#39;Page Title&#39;)

@section(&#39;sidebar&#39;)
    @@parent

    &lt;p&gt;This is appended to the master sidebar.&lt;/p&gt;
@endsection

@section(&#39;content&#39;)
    &lt;p&gt;This is my body content.&lt;/p&gt;
@endsection
</code></pre>
<p>在此範例中，<code>sidebar</code> 區段使用 <code>@@parent</code> 指令將內容附加（而不是覆蓋）到佈局的側邊欄。<code>@@parent</code> 指令將在視圖渲染時被佈局的內容替換。</p>
<blockquote>
<p>[!NOTE]
與上面的範例相反，此 <code>sidebar</code> 區段以 <code>@endsection</code> 結尾，而不是 <code>@show</code>。<code>@endsection</code> 指令將只定義一個區段，而 <code>@show</code> 將定義並<strong>立即 yield</strong> 該區段。</p>
</blockquote>
<p><code>@yield</code> 指令還接受一個預設值作為其第二個參數。如果正在 yield 的區段未定義，則將渲染此值：</p>
<pre><code class="language-blade">@yield(&#39;content&#39;, &#39;Default content&#39;)
</code></pre>
<p><a name="forms"></a></p>
<h2>表單</h2>
<p><a name="csrf-field"></a></p>
<h3>CSRF 欄位</h3>
<p>每當您定義一個 HTML 表單時，您應該在表單中包含一個隱藏的 CSRF 令牌欄位，以便<a href="/docs/csrf"> CSRF 保護</a>中介層可以驗證請求。您可以使用 <code>@csrf</code> Blade 指令來生成令牌欄位：</p>
<pre><code class="language-blade">&lt;form method=&quot;POST&quot; action=&quot;/profile&quot;&gt;
    @csrf

    ...
&lt;/form&gt;
</code></pre>
<p><a name="method-field"></a></p>
<h3>方法欄位</h3>
<p>由於 HTML 表單不能發出 <code>PUT</code>、<code>PATCH</code> 或 <code>DELETE</code> 請求，您將需要添加一個隱藏的 <code>_method</code> 欄位來欺騙這些 HTTP 動詞。<code>@method</code> Blade 指令可以為您創建此欄位：</p>
<pre><code class="language-blade">&lt;form action=&quot;/foo/bar&quot; method=&quot;POST&quot;&gt;
    @method(&#39;PUT&#39;)

    ...
&lt;/form&gt;
</code></pre>
<p><a name="validation-errors"></a></p>
<h3>驗證錯誤</h3>
<p><code>@error</code> 指令可用於快速檢查給定屬性是否存在<a href="/docs/validation#quick-displaying-the-validation-errors">驗證錯誤消息</a>。在 <code>@error</code> 指令中，您可以 echo <code>$message</code> 變數來顯示錯誤消息：</p>
<pre><code class="language-blade">&lt;!-- /resources/views/post/create.blade.php --&gt;

&lt;label for=&quot;title&quot;&gt;Post Title&lt;/label&gt;

&lt;input
    id=&quot;title&quot;
    type=&quot;text&quot;
    class=&quot;@error(&#39;title&#39;) is-invalid @enderror&quot;
/&gt;

@error(&#39;title&#39;)
    &lt;div class=&quot;alert alert-danger&quot;&gt;{{ $message }}&lt;/div&gt;
@enderror
</code></pre>
<p>由於 <code>@error</code> 指令編譯為「if」語句，您可以使用 <code>@else</code> 指令在屬性沒有錯誤時渲染內容：</p>
<pre><code class="language-blade">&lt;!-- /resources/views/auth.blade.php --&gt;

&lt;label for=&quot;email&quot;&gt;Email address&lt;/label&gt;

&lt;input
    id=&quot;email&quot;
    type=&quot;email&quot;
    class=&quot;@error(&#39;email&#39;) is-invalid @else is-valid @enderror&quot;
/&gt;
</code></pre>
<p>您可以將<a href="/docs/validation#named-error-bags">特定錯誤包的名稱</a>作為第二個參數傳遞給 <code>@error</code> 指令，以在包含多個表單的頁面上檢索驗證錯誤消息：</p>
<pre><code class="language-blade">&lt;!-- /resources/views/auth.blade.php --&gt;

&lt;label for=&quot;email&quot;&gt;Email address&lt;/label&gt;

&lt;input
    id=&quot;email&quot;
    type=&quot;email&quot;
    class=&quot;@error(&#39;email&#39;, &#39;login&#39;) is-invalid @enderror&quot;
/&gt;

@error(&#39;email&#39;, &#39;login&#39;)
    &lt;div class=&quot;alert alert-danger&quot;&gt;{{ $message }}&lt;/div&gt;
@enderror
</code></pre>
<p><a name="stacks"></a></p>
<h2>堆疊</h2>
<p>Blade 允許您推送到命名堆疊，該堆疊可以在另一個視圖或佈局的其他地方渲染。這對於指定子視圖所需的任何 JavaScript 函式庫特別有用：</p>
<pre><code class="language-blade">@push(&#39;scripts&#39;)
    &lt;script src=&quot;/example.js&quot;&gt;&lt;/script&gt;
@endpush
</code></pre>
<p>如果您想在給定的布林表達式評估為 <code>true</code> 時 <code>@push</code> 內容，您可以使用 <code>@pushIf</code> 指令：</p>
<pre><code class="language-blade">@pushIf($shouldPush, &#39;scripts&#39;)
    &lt;script src=&quot;/example.js&quot;&gt;&lt;/script&gt;
@endPushIf
</code></pre>
<p>您可以根據需要多次推送到堆疊。要渲染完整的堆疊內容，請將堆疊的名稱傳遞給 <code>@stack</code> 指令：</p>
<pre><code class="language-blade">&lt;head&gt;
    &lt;!-- Head Contents --&gt;

    @stack(&#39;scripts&#39;)
&lt;/head&gt;
</code></pre>
<p>如果您想在堆疊的開頭前置內容，您應該使用 <code>@prepend</code> 指令：</p>
<pre><code class="language-blade">@push(&#39;scripts&#39;)
    This will be second...
@endpush

// Later...

@prepend(&#39;scripts&#39;)
    This will be first...
@endprepend
</code></pre>
<p><code>@hasstack</code> 指令可用於判斷堆疊是否為空：</p>
<pre><code class="language-blade">@hasstack(&#39;list&#39;)
    &lt;ul&gt;
        @stack(&#39;list&#39;)
    &lt;/ul&gt;
@endif
</code></pre>
<p><a name="service-injection"></a></p>
<h2>服務注入</h2>
<p><code>@inject</code> 指令可用於從 Laravel <a href="/docs/container">服務容器</a>中檢索服務。傳遞給 <code>@inject</code> 的第一個參數是服務將被放置的變數名稱，而第二個參數是您希望解析的服務的類別或介面名稱：</p>
<pre><code class="language-blade">@inject(&#39;metrics&#39;, &#39;App\\Services\\MetricsService&#39;)

&lt;div&gt;
    Monthly Revenue: {{ $metrics-&gt;monthlyRevenue() }}.
&lt;/div&gt;
</code></pre>
<p><a name="rendering-inline-blade-templates"></a></p>
<h2>渲染內聯 Blade 範本</h2>
<p>有時您可能需要將原始 Blade 範本字串轉換為有效的 HTML。您可以使用 <code>Blade</code> Facade 提供的 <code>render</code> 方法來完成此操作。<code>render</code> 方法接受 Blade 範本字串和一個可選的資料數組來提供給模板：</p>
<pre><code class="language-php">use Illuminate\\Support\\Facades\\Blade;

return Blade::render(&#39;Hello, {{ $name }}&#39;, [&#39;name&#39; =&gt; &#39;Julian Bashir&#39;]);
</code></pre>
<p>Laravel 透過將內聯 Blade 範本寫入 <code>storage/framework/views</code> 目錄來渲染它們。如果您希望 Laravel 在渲染 Blade 範本後移除這些臨時檔案，您可以向方法提供 <code>deleteCachedView</code> 參數：</p>
<pre><code class="language-php">return Blade::render(
    &#39;Hello, {{ $name }}&#39;,
    [&#39;name&#39; =&gt; &#39;Julian Bashir&#39;],
    deleteCachedView: true
);
</code></pre>
<p><a name="rendering-blade-fragments"></a></p>
<h2>渲染 Blade 片段</h2>
<p>使用像 <a href="https://turbo.hotwired.dev/">Turbo</a> 和 <a href="https://htmx.org/">htmx</a> 這樣的前端框架時，您可能偶爾需要僅在 HTTP 響應中返回 Blade 模板的一部分。Blade「片段」允許您做到這一點。首先，將 Blade 模板的一部分放在 <code>@fragment</code> 和 <code>@endfragment</code> 指令中：</p>
<pre><code class="language-blade">@fragment(&#39;user-list&#39;)
    &lt;ul&gt;
        @foreach ($users as $user)
            &lt;li&gt;{{ $user-&gt;name }}&lt;/li&gt;
        @endforeach
    &lt;/ul&gt;
@endfragment
</code></pre>
<p>然後，在渲染使用此模板的視圖時，您可以調用 <code>fragment</code> 方法來指定僅應在傳出 HTTP 響應中包含指定的片段：</p>
<pre><code class="language-php">return view(&#39;dashboard&#39;, [&#39;users&#39; =&gt; $users])-&gt;fragment(&#39;user-list&#39;);
</code></pre>
<p><code>fragmentIf</code> 方法允許您根據給定條件有條件地返回視圖的片段。否則，將返回整個視圖：</p>
<pre><code class="language-php">return view(&#39;dashboard&#39;, [&#39;users&#39; =&gt; $users])
    -&gt;fragmentIf($request-&gt;hasHeader(&#39;HX-Request&#39;), &#39;user-list&#39;);
</code></pre>
<p><code>fragments</code> 和 <code>fragmentsIf</code> 方法允許您在響應中返回多個視圖片段。片段將被連接在一起：</p>
<pre><code class="language-php">view(&#39;dashboard&#39;, [&#39;users&#39; =&gt; $users])
    -&gt;fragments([&#39;user-list&#39;, &#39;comment-list&#39;]);

view(&#39;dashboard&#39;, [&#39;users&#39; =&gt; $users])
    -&gt;fragmentsIf(
        $request-&gt;hasHeader(&#39;HX-Request&#39;),
        [&#39;user-list&#39;, &#39;comment-list&#39;]
    );
</code></pre>
<p><a name="extending-blade"></a></p>
<h2>擴展 Blade</h2>
<p>Blade 允許您使用 <code>directive</code> 方法定義自己的自訂指令。當 Blade 編譯器遇到自訂指令時，它將使用指令包含的表達式呼叫提供的回調。</p>
<p>以下範例創建一個 <code>@datetime($var)</code> 指令，該指令格式化給定的 <code>$var</code>，它應該是 <code>DateTime</code> 的實例：</p>
<pre><code class="language-php">&lt;?php

namespace App\\Providers;

use Illuminate\\Support\\Facades\\Blade;
use Illuminate\\Support\\ServiceProvider;

class AppServiceProvider extends ServiceProvider
{
    /**
     * 註冊任何應用程式服務。
     */
    public function register(): void
    {
        // ...
    }

    /**
     * 引導任何應用程式服務。
     */
    public function boot(): void
    {
        Blade::directive(&#39;datetime&#39;, function (string $expression) {
            return &quot;&lt;?php echo ($expression)-&gt;format(&#39;m/d/Y H:i&#39;); ?&gt;&quot;;
        });
    }
}
</code></pre>
<p>如您所見，我們將 <code>format</code> 方法鏈接到傳遞給指令的任何表達式上。因此，在此範例中，此指令生成的最終 PHP 將是：</p>
<pre><code class="language-php">&lt;?php echo ($var)-&gt;format(&#39;m/d/Y H:i&#39;); ?&gt;
</code></pre>
<blockquote>
<p>[!WARNING]
更新 Blade 指令的邏輯後，您需要刪除所有快取的 Blade 視圖。可以使用 <code>view:clear</code> Artisan 命令移除快取的 Blade 視圖。</p>
</blockquote>
<p><a name="custom-echo-handlers"></a></p>
<h3>自訂 Echo 處理器</h3>
<p>如果您嘗試使用 Blade「echo」一個物件，將會呼叫物件的 <code>__toString</code> 方法。<code>__toString</code> 方法是 PHP 的內建「魔法方法」之一。但是，有時您可能無法控制給定類別的 <code>__toString</code> 方法，例如當與您互動的類別屬於第三方函式庫時。</p>
<p>在這些情況下，Blade 允許您為該特定類型的物件註冊一個自訂 echo 處理器。為此，您應該呼叫 Blade 的 <code>stringable</code> 方法。<code>stringable</code> 方法接受一個閉包。此閉包應類型提示其負責渲染的物件類型。通常，<code>stringable</code> 方法應在應用程式的 <code>AppServiceProvider</code> 類別的 <code>boot</code> 方法中呼叫：</p>
<pre><code class="language-php">use Illuminate\\Support\\Facades\\Blade;
use Money\\Money;

/**
 * 引導任何應用程式服務。
 */
public function boot(): void
{
    Blade::stringable(function (Money $money) {
        return $money-&gt;formatTo(&#39;en_GB&#39;);
    });
}
</code></pre>
<p>定義自訂 echo 處理器後，您可以直接在 Blade 範本中 echo 該物件：</p>
<pre><code class="language-blade">Cost: {{ $money }}
</code></pre>
<p><a name="custom-if-statements"></a></p>
<h3>自訂 If 語句</h3>
<p>編程自訂指令有時比定義簡單的自訂條件語句更複雜。出於這個原因，Blade 提供了一個 <code>Blade::if</code> 方法，該方法允許您使用閉包快速定義自訂條件指令。例如，讓我們定義一個檢查應用程式配置的預設「磁碟」的自訂條件。我們可以在 <code>AppServiceProvider</code> 的 <code>boot</code> 方法中完成此操作：</p>
<pre><code class="language-php">use Illuminate\\Support\\Facades\\Blade;

/**
 * 引導任何應用程式服務。
 */
public function boot(): void
{
    Blade::if(&#39;disk&#39;, function (string $value) {
        return config(&#39;filesystems.default&#39;) === $value;
    });
}
</code></pre>
<p>定義自訂條件後，您可以在範本中使用它：</p>
<pre><code class="language-blade">@disk(&#39;local&#39;)
    &lt;!-- 應用程式正在使用本地磁碟... --&gt;
@elsedisk(&#39;s3&#39;)
    &lt;!-- 應用程式正在使用 s3 磁碟... --&gt;
@else
    &lt;!-- 應用程式正在使用其他磁碟... --&gt;
@enddisk

@unlessdisk(&#39;local&#39;)
    &lt;!-- 應用程式未使用本地磁碟... --&gt;
@enddisk
</code></pre>
`;
---

<DocsLayout title="Blade 範本" currentPath="blade">
  <div class="mb-8">
    <nav class="text-sm text-text-secondary mb-4">
      <a href="/" class="hover:text-accent">首頁</a>
      <span class="mx-2">/</span>
      <span>文件</span>
      <span class="mx-2">/</span>
      <span>Blade 範本</span>
    </nav>
    
    <h1 class="text-4xl font-bold mb-4">Blade 範本</h1>
    
    <div class="bg-bg-secondary border border-border rounded-lg p-4 mb-6">
      <p class="text-text-secondary">
        📝 此頁面為 Laravel 官方文檔的繁體中文翻譯。
        <a href="https://laravel.com/docs/13.x/blade" class="text-accent" target="_blank" rel="noopener">
          查看原始英文版本
        </a>
      </p>
    </div>
  </div>

  <div class="prose prose-invert max-w-none" set:html={content}></div>
</DocsLayout>
