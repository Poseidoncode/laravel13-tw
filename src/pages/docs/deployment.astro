---
import DocsLayout from '../../layouts/DocsLayout.astro';

const content = `<h1>部署</h1>
<ul>
<li><a href="#introduction">簡介</a></li>
<li><a href="#server-requirements">伺服器需求</a></li>
<li><a href="#server-configuration">伺服器配置</a><ul>
<li><a href="#nginx">Nginx</a></li>
<li><a href="#frankenphp">FrankenPHP</a></li>
<li><a href="#directory-permissions">目錄權限</a></li>
</ul>
</li>
<li><a href="#optimization">優化</a><ul>
<li><a href="#optimizing-configuration-loading">快取配置</a></li>
<li><a href="#caching-events">快取事件</a></li>
<li><a href="#optimizing-route-loading">快取路由</a></li>
<li><a href="#optimizing-view-loading">快取視圖</a></li>
</ul>
</li>
<li><a href="#reloading-services">重新載入服務</a></li>
<li><a href="#debug-mode">除錯模式</a></li>
<li><a href="#the-health-route">健康檢查路由</a></li>
<li><a href="#deploying-with-cloud-or-forge">使用 Laravel Cloud 或 Forge 部署</a></li>
</ul>
<p><a name="introduction"></a></p>
<h2>簡介</h2>
<p>當您準備將 Laravel 應用程式部署到生產環境時，您可以做一些重要的事情來確保應用程式以盡可能高效的方式運行。在本文檔中，我們將介紹一些確保 Laravel 應用程式正確部署的良好起點。</p>
<p><a name="server-requirements"></a></p>
<h2>伺服器需求</h2>
<p>Laravel 框架有一些系統需求。您應該確保您的 Web 伺服器具有以下最低 PHP 版本和擴充套件：</p>
<div class="content-list" markdown="1"><ul>
<li>PHP &gt;= 8.3</li>
<li>Ctype PHP Extension</li>
<li>cURL PHP Extension</li>
<li>DOM PHP Extension</li>
<li>Fileinfo PHP Extension</li>
<li>Filter PHP Extension</li>
<li>Hash PHP Extension</li>
<li>Mbstring PHP Extension</li>
<li>OpenSSL PHP Extension</li>
<li>PCRE PHP Extension</li>
<li>PDO PHP Extension</li>
<li>Session PHP Extension</li>
<li>Tokenizer PHP Extension</li>
<li>XML PHP Extension</li>
</ul>
</div><p><a name="server-configuration"></a></p>
<h2>伺服器配置</h2>
<p><a name="nginx"></a></p>
<h3>Nginx</h3>
<p>如果您正在將應用程式部署到運行 Nginx 的伺服器，您可以使用以下配置檔案作為配置 Web 伺服器的起點。大多數情況下，此檔案需要根據您伺服器的配置進行自訂。<strong>如果您需要協助管理伺服器，請考慮使用像 <a href="https://cloud.laravel.com">Laravel Cloud</a> 這樣的全託管 Laravel 平台。</strong></p>
<p>請確保像下面的配置一樣，您的 Web 伺服器將所有請求導向應用程式的 <code>public/index.php</code> 檔案。您不應嘗試將 <code>index.php</code> 檔案移動到專案的根目錄，因為從專案根目錄提供服務將使許多敏感配置檔案暴露在公共網路上：</p>
<pre><code class="language-nginx">server {
    listen 80;
    listen [::]:80;
    server_name example.com;
    root /srv/example.com/public;

    add_header X-Frame-Options &quot;SAMEORIGIN&quot;;
    add_header X-Content-Type-Options &quot;nosniff&quot;;

    index index.php;

    charset utf-8;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    error_page 404 /index.php;

    location ~ ^/index\\.php(/|$) {
        fastcgi_pass unix:/var/run/php/php8.3-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_hide_header X-Powered-By;
    }

    location ~ /\\.(?!well-known).* {
        deny all;
    }
}
</code></pre>
<p><a name="frankenphp"></a></p>
<h3>FrankenPHP</h3>
<p><a href="https://frankenphp.dev/">FrankenPHP</a> 也可用於提供您的 Laravel 應用程式服務。FrankenPHP 是一個用 Go 編寫的現代 PHP 應用程式伺服器。要使用 FrankenPHP 提供 Laravel PHP 應用程式服務，您只需調用其 <code>php-server</code> 命令：</p>
<pre><code class="language-shell">frankenphp php-server -r public/
</code></pre>
<p>要利用 FrankenPHP 支援的更強大功能，例如其 <a href="/docs/octane">Laravel Octane</a> 整合、HTTP/3、現代壓縮或將 Laravel 應用程式打包為獨立二進制檔案的能力，請查閱 FrankenPHP 的 <a href="https://frankenphp.dev/docs/laravel/">Laravel 文檔</a>。</p>
<p><a name="directory-permissions"></a></p>
<h3>目錄權限</h3>
<p>Laravel 需要寫入 <code>bootstrap/cache</code> 和 <code>storage</code> 目錄，因此您應該確保 Web 伺服器程序所有者具有寫入這些目錄的權限。</p>
<p><a name="optimization"></a></p>
<h2>優化</h2>
<p>將應用程式部署到生產環境時，有多種檔案應該被快取，包括配置、事件、路由和視圖。Laravel 提供了一個單一、便利的 <code>optimize</code> Artisan 命令，它將快取所有這些檔案。此命令通常應作為應用程式部署過程的一部分來調用：</p>
<pre><code class="language-shell">php artisan optimize
</code></pre>
<p><code>optimize:clear</code> 方法可用於移除 <code>optimize</code> 命令生成的所有快取檔案以及預設快取驅動程式中的所有金鑰：</p>
<pre><code class="language-shell">php artisan optimize:clear
</code></pre>
<p>在以下文檔中，我們將討論 <code>optimize</code> 命令執行的每個細粒度優化命令。</p>
<p><a name="optimizing-configuration-loading"></a></p>
<h3>快取配置</h3>
<p>將應用程式部署到生產環境時，您應該確保在部署過程中運行 <code>config:cache</code> Artisan 命令：</p>
<pre><code class="language-shell">php artisan config:cache
</code></pre>
<p>此命令將 Laravel 的所有配置檔案合併到一個快取檔案中，大大減少了框架在載入配置值時必須對檔案系統進行的訪問次數。</p>
<blockquote>
<p>[!WARNING]
如果您在部署過程中執行 <code>config:cache</code> 命令，您應該確保只從配置檔案中調用 <code>env</code> 函數。一旦配置被快取，<code>.env</code> 檔案將不會被載入，所有對 <code>.env</code> 變數的 <code>env</code> 函數呼叫都將返回 <code>null</code>。</p>
</blockquote>
<p><a name="caching-events"></a></p>
<h3>快取事件</h3>
<p>您應該在部署過程中快取應用程式的自動偵測事件到監聽器映射。這可以透過在部署期間調用 <code>event:cache</code> Artisan 命令來實現：</p>
<pre><code class="language-shell">php artisan event:cache
</code></pre>
<p><a name="optimizing-route-loading"></a></p>
<h3>快取路由</h3>
<p>如果您正在構建一個具有許多路由的大型應用程式，您應該確保在部署過程中運行 <code>route:cache</code> Artisan 命令：</p>
<pre><code class="language-shell">php artisan route:cache
</code></pre>
<p>此命令將您所有的路由註冊簡化為快取檔案中的單個方法呼叫，從而在註冊數百個路由時提高路由註冊的效能。</p>
<p><a name="optimizing-view-loading"></a></p>
<h3>快取視圖</h3>
<p>將應用程式部署到生產環境時，您應該確保在部署過程中運行 <code>view:cache</code> Artisan 命令：</p>
<pre><code class="language-shell">php artisan view:cache
</code></pre>
<p>此命令會預編譯您所有的 Blade 視圖，這樣它們就不會按需編譯，從而提高每個返回視圖的請求的效能。</p>
<p><a name="reloading-services"></a></p>
<h2>重新載入服務</h2>
<blockquote>
<p>[!NOTE]
部署到 <a href="https://cloud.laravel.com">Laravel Cloud</a> 時，不需要使用 <code>reload</code> 命令，因為所有服務的優雅重新載入會自動處理。</p>
</blockquote>
<p>部署應用程式的新版本後，任何長時間運行的服務（如佇列工作者、Laravel Reverb 或 Laravel Octane）都應重新載入/重新啟動以使用新程式碼。Laravel 提供了一個單一的 <code>reload</code> Artisan 命令來終止這些服務：</p>
<pre><code class="language-shell">php artisan reload
</code></pre>
<p>如果您沒有使用 <a href="https://cloud.laravel.com">Laravel Cloud</a>，您應該手動配置一個程序監控器，以便偵測可重新載入程序何時退出並自動重新啟動它們。</p>
<p><a name="debug-mode"></a></p>
<h2>除錯模式</h2>
<p><code>config/app.php</code> 配置檔案中的除錯選項決定實際向用戶顯示多少錯誤資訊。預設情況下，此選項設定為尊重應用程式 <code>.env</code> 檔案中儲存的 <code>APP_DEBUG</code> 環境變數的值。</p>
<blockquote>
<p>[!WARNING]
<strong>在生產環境中，此值應始終為 <code>false</code>。如果在生產中將 <code>APP_DEBUG</code> 變數設定為 <code>true</code>，您將面臨向應用程式的最終用戶暴露敏感配置值的風險。</strong></p>
</blockquote>
<p><a name="the-health-route"></a></p>
<h2>健康檢查路由</h2>
<p>Laravel 包含一個內建的健康檢查路由，可用於監控應用程式的狀態。在生產環境中，此路由可用於向正常運行時間監控器、負載平衡器或像 Kubernetes 這樣的編排系統報告應用程式的狀態。</p>
<p>預設情況下，健康檢查路由在 <code>/up</code> 提供服務，如果應用程式在沒有異常的情況下啟動，將返回 200 HTTP 響應。否則，將返回 500 HTTP 響應。您可以在應用程式的 <code>bootstrap/app</code> 檔案中配置此路由的 URI：</p>
<pre><code class="language-php">-&gt;withRouting(
    web: __DIR__.&#39;/../routes/web.php&#39;,
    commands: __DIR__.&#39;/../routes/console.php&#39;,
    health: &#39;/up&#39;, // [tl! remove]
    health: &#39;/status&#39;, // [tl! add]
)
</code></pre>
<p>當對此路由發出 HTTP 請求時，Laravel 還會派發一個 <code>Illuminate\\Foundation\\Events\\DiagnosingHealth</code> 事件，允許您執行與應用程式相關的額外健康檢查。在<a href="/docs/events">監聽器</a>中，您可以檢查應用程式的資料庫或快取狀態。如果您偵測到應用程式的問題，可以從監聽器中拋出異常。</p>
<p><a name="deploying-with-cloud-or-forge"></a></p>
<h2>使用 Laravel Cloud 或 Forge 部署</h2>
<p><a name="laravel-cloud"></a></p>
<h4>Laravel Cloud</h4>
<p>如果您想要一個專為 Laravel 調校的全託管、自動擴展部署平台，請查看 <a href="https://cloud.laravel.com">Laravel Cloud</a>。Laravel Cloud 是一個強大的 Laravel 部署平台，提供託管計算、資料庫、快取和物件儲存。</p>
<p>在 Cloud 上啟動您的 Laravel 應用程式，並愛上可擴展的簡潔性。Laravel Cloud 由 Laravel 的創建者精心調校，與框架無縫配合，因此您可以繼續完全按照您習慣的方式編寫 Laravel 應用程式。</p>
<p><a name="laravel-forge"></a></p>
<h4>Laravel Forge</h4>
<p>如果您偏好管理自己的伺服器，但對於配置運行強大 Laravel 應用程式所需的各種服務不太舒適，<a href="https://forge.laravel.com">Laravel Forge</a> 是一個用於 Laravel 應用程式的 VPS 伺服器管理平台。</p>
<p>Laravel Forge 可以在各種基礎架構提供者（如 DigitalOcean、Linode、AWS 等）上創建伺服器。此外，Forge 安裝和管理構建強大 Laravel 應用程式所需的所有工具，如 Nginx、MySQL、Redis、Memcached、Beanstalk 等。</p>
`;
---

<DocsLayout title="部署" currentPath="deployment">
  <div class="mb-8">
    <nav class="text-sm text-text-secondary mb-4">
      <a href="/" class="hover:text-accent">首頁</a>
      <span class="mx-2">/</span>
      <span>文件</span>
      <span class="mx-2">/</span>
      <span>部署</span>
    </nav>
    
    <h1 class="text-4xl font-bold mb-4">部署</h1>
    
    <div class="bg-bg-secondary border border-border rounded-lg p-4 mb-6">
      <p class="text-text-secondary">
        📝 此頁面為 Laravel 官方文檔的繁體中文翻譯。
        <a href="https://laravel.com/docs/13.x/deployment" class="text-accent" target="_blank" rel="noopener">
          查看原始英文版本
        </a>
      </p>
    </div>
  </div>

  <div class="prose prose-invert max-w-none" set:html={content}></div>
</DocsLayout>
