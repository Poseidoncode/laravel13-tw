{
  "name": "laravel13-tw",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist && rm -rf public/pagefind && cp -r dist/pagefind public/pagefind",
    "build:astro": "astro build",
    "build:search": "pagefind --site dist && rm -rf public/pagefind && cp -r dist/pagefind public/pagefind",
    "dev:setup": "echo 'Run \"npm run build\" first to generate search index';",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.0",
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/vite": "^4.3.1",
    "astro": "^7.0.3",
    "marked": "^18.0.5",
    "tailwindcss": "^4.3.1"
  },
  "devDependencies": {
    "@pagefind/default-ui": "^1.5.2",
    "pagefind": "^1.5.2"
  }
}
